# Nested repositories are tracked and pushed independently during Phase C.0.
# DEC-0005 (2026-08-31): apps/reader and services/uni-rag squash-imported into
# this repo; only vibereader-macos remains a nested repo (own local .git).
/apps/vibereader-macos/

# Runtime and local noise
.DS_Store
node_modules/
dist/
build/
*.log
*.tmp

# Keep shared contracts and lifecycle artifacts tracked here.
!/packages/
!/docs/
!/.ship/
!/scripts/
test-results/
