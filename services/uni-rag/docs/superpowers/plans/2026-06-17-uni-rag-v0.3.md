# uni-rag v0.3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.1 + v0.2 已稳定 (51 tests passing, 96% coverage, 28 commits)。v0.3 在 v0.2 之上加 3 个独立特性：(1) 导出答案到 Markdown / PDF、(2) Docker 一键部署、(3) 跨 session 的多知识库（KB）支持。学生一个 KB 装一门课，几门课互不污染。

**Architecture:** 完全沿用 v0.1 + v0.2 的 RAG pipeline 和存储接口，不动核心检索 / 生成流程。Feature 1 加 `src/uni_rag/export/` 模块 + `GET /api/sessions/{sid}/messages/{idx}/export` + 前端 Download 按钮。Feature 2 加 `Dockerfile` + `docker-compose.yml` + `.dockerignore`。Feature 3 重构 `VectorStore` / `BM25Index` 接受 `collection_name` / `index_dir` 参数，加 `KBStore` (SQLite) + 多 KB 路由 + CLI `kb` 子命令组，v0.2 单 KB 模式通过"default KB"透明保留。

**Tech Stack:** 同 v0.1 + v0.2。Feature 1 PDF 渲染选 `weasyprint`（纯 Python，依赖 libpango，理由：本机 macOS + Docker slim 都好装；`pdfkit` 需要 wkhtmltopdf 二进制，跨平台装包更痛苦；markdown-it-py 把 MD → HTML 后 weasyprint 一行转 PDF）。Feature 2 Docker 用 `python:3.13-slim` + `uv`（不引入 poetry/pip-tools）。Feature 3 SQLite 新表 `knowledge_bases` + `session_kbs` 关联表，Chroma 每 KB 一个 collection。

**BDD Coverage:**
- **B8 导出答案到 MD / PDF** → M9
- **B9 Docker 一键部署** → M10
- **B10 多 KB 隔离** → M11

---

## File Structure

v0.3 涉及的新增/修改文件（其余沿用 v0.1 + v0.2）：

```
uni-rag/
├── src/uni_rag/
│   ├── config.py                  # M11: +kb_dir_path
│   ├── store/
│   │   ├── vector.py              # M11: 接受 collection_name 参数
│   │   └── bm25.py                # M11: 接受 index_dir 参数（已支持）
│   ├── rag/pipeline.py            # M11: 接受 kb_id 参数
│   ├── retrieve/retriever.py      # M11: 接受 kb_id 参数
│   ├── ingest/pipeline.py         # M11: 接受 kb_id 参数
│   ├── session/store.py           # M11: +session_kbs 关联 + bind_kbs
│   ├── store/kb.py                # M11: 新增 KBStore
│   ├── export/                    # M9: 新增模块
│   │   ├── __init__.py
│   │   ├── md_exporter.py
│   │   └── pdf_exporter.py
│   ├── api/
│   │   ├── routes.py              # M9 export + M11 kb 路由
│   │   └── schemas.py             # M9 + M11 schemas
│   └── web/
│       ├── index.html             # M11: KB 切换 UI
│       ├── app.js                 # M9 download + M11 kb UI
│       └── style.css              # M11: KB 选择器样式
├── cli/
│   └── main.py                    # M11: +kb 子命令组
├── tests/
│   ├── unit/
│   │   ├── test_export.py         # M9: 新增
│   │   └── test_kb.py             # M11: 新增
│   ├── integration/
│   │   ├── test_api.py            # M9 export + M11 kb endpoints
│   │   └── test_query_pipeline.py # M11: 多 KB 隔离
│   └── bdd/
│       └── test_b8_b10.py         # M9 + M11 端到端
├── Dockerfile                     # M10: 新增
├── .dockerignore                  # M10: 新增
└── docker-compose.yml             # M10: 新增
```

---

## Milestone 9: 导出答案 (B8)

**目的:** 把单条 assistant 答案（含问题、答案、引用）落盘成 Markdown 或 PDF，方便学生做作业 / 笔记。

**PDF 库选型：** `weasyprint`（≥60.0）。理由：
1. 纯 Python API，`HTML(string=html).write_pdf()` 一行
2. 渲染质量好，CSS 支持完整（含中文字体）
3. Docker slim 镜像装 `libpango-1.0-0` + `libpangoft2-1.0-0` 即可，apt 包小
4. `pdfkit` 需要系统装 `wkhtmltopdf` 二进制（~50MB），跨平台麻烦；`reportlab` 是 low-level API 写 PDF 还要手算位置

### Task 9.1: 加 weasyprint 依赖

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: 写测试 (sanity check 库装上)**

```python
# tests/unit/test_export.py
def test_weasyprint_importable():
    import weasyprint  # noqa: F401
    assert weasyprint.__version__ >= "60.0"
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_export.py -v`
Expected: FAIL with `ModuleNotFoundError`

- [ ] **Step 3: 改 pyproject.toml**

在 `dependencies` 末尾追加:

```toml
    "weasyprint>=60.0",
```

- [ ] **Step 4: uv sync + 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv sync && uv run pytest tests/unit/test_export.py -v`
Expected: 1 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add pyproject.toml uv.lock tests/unit/test_export.py
git commit -m "chore(deps): add weasyprint for PDF export"
```

### Task 9.2: MD exporter 纯函数

**Files:**
- Create: `src/uni_rag/export/__init__.py`
- Create: `src/uni_rag/export/md_exporter.py`
- Modify: `tests/unit/test_export.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/unit/test_export.py`:

```python
from uni_rag.export.md_exporter import render_markdown


def test_render_markdown_includes_question_and_answer():
    payload = {
        "question": "什么是监督学习？",
        "answer": "监督学习使用标注数据 [abc123:100]。",
        "citations": [
            {
                "chunk_id": "abc123:100",
                "source": "chapter1.pdf",
                "section": "1.1",
                "text": "Supervised learning uses labeled data.",
            }
        ],
    }
    md = render_markdown(payload)
    assert "# 问答记录" in md
    assert "## 问题" in md
    assert "什么是监督学习？" in md
    assert "## 答案" in md
    assert "监督学习使用标注数据" in md
    assert "## 引用" in md
    assert "chapter1.pdf" in md
    assert "1.1" in md
    assert "Supervised learning uses labeled data." in md


def test_render_markdown_handles_no_citations():
    payload = {"question": "q", "answer": "a", "citations": []}
    md = render_markdown(payload)
    assert "（无引用）" in md or "无引用" in md


def test_render_markdown_escapes_md_special_chars_in_citation_text():
    payload = {
        "question": "q",
        "answer": "a",
        "citations": [
            {
                "chunk_id": "x:0",
                "source": "f.md",
                "section": "",
                "text": "# header | pipe *italic*",
            }
        ],
    }
    md = render_markdown(payload)
    # 引用文本应被包到 code block 避免 markdown 注入
    assert "```" in md
    assert "# header | pipe *italic*" in md
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_export.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'uni_rag.export'`）

- [ ] **Step 3: 写 src/uni_rag/export/__init__.py**

```python
"""Export answers to Markdown or PDF."""
```

- [ ] **Step 4: 写 src/uni_rag/export/md_exporter.py**

```python
"""Render a query payload (question + answer + citations) to Markdown."""
from __future__ import annotations
from datetime import datetime, timezone
from typing import TypedDict


class CitationPayload(TypedDict):
    chunk_id: str
    source: str
    section: str
    text: str
    span: tuple[int, int] | None


class ExportPayload(TypedDict):
    question: str
    answer: str
    citations: list[CitationPayload]


def render_markdown(payload: ExportPayload) -> str:
    """Return a clean Markdown string of the Q&A + citations."""
    now = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M UTC")
    parts: list[str] = []
    parts.append("# 问答记录")
    parts.append(f"\n_导出时间：{now}_\n")
    parts.append("## 问题\n")
    parts.append(payload["question"].strip() + "\n")
    parts.append("## 答案\n")
    parts.append(payload["answer"].strip() + "\n")
    citations = payload.get("citations") or []
    if not citations:
        parts.append("## 引用\n\n（无引用）\n")
        return "\n".join(parts)
    parts.append("## 引用\n")
    for i, c in enumerate(citations, 1):
        src = c.get("source", "unknown")
        section = c.get("section", "")
        chunk_id = c.get("chunk_id", "")
        text = c.get("text", "")
        header = f"{i}. **{src}**"
        if section:
            header += f" — {section}"
        if chunk_id:
            header += f"  \n_chunk: `{chunk_id}`_"
        parts.append(header)
        parts.append("")
        # 引用原文包到 code block，避免 markdown 特殊字符（# | * _ 等）被错误渲染
        parts.append("```")
        parts.append(text.strip())
        parts.append("```")
        parts.append("")
    return "\n".join(parts)
```

- [ ] **Step 5: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_export.py -v`
Expected: 4 passed (含 weasyprint import + 3 个新)

- [ ] **Step 6: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/export/ tests/unit/test_export.py
git commit -m "feat(export): Markdown exporter for Q&A + citations"
```

### Task 9.3: PDF exporter

**Files:**
- Create: `src/uni_rag/export/pdf_exporter.py`
- Modify: `tests/unit/test_export.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/unit/test_export.py`:

```python
from uni_rag.export.pdf_exporter import render_pdf


def test_render_pdf_returns_valid_pdf_bytes():
    payload = {
        "question": "什么是监督学习？",
        "answer": "监督学习使用标注数据。",
        "citations": [
            {
                "chunk_id": "x:0",
                "source": "chapter1.pdf",
                "section": "1.1",
                "text": "supervised",
            }
        ],
    }
    pdf_bytes = render_pdf(payload)
    assert isinstance(pdf_bytes, bytes)
    assert len(pdf_bytes) > 100
    assert pdf_bytes.startswith(b"%PDF-")
    assert pdf_bytes.rstrip().endswith(b"%%EOF") or b"%%EOF" in pdf_bytes[-1024:]


def test_render_pdf_handles_chinese_text():
    payload = {
        "question": "中文问题",
        "answer": "中文答案包含引用 [x:0]",
        "citations": [
            {"chunk_id": "x:0", "source": "s.pdf", "section": "1", "text": "中文 chunk"}
        ],
    }
    pdf_bytes = render_pdf(payload)
    assert pdf_bytes.startswith(b"%PDF-")
    # PDF 内文字符存为字节流，无法直接 assert 文本，但 bytes 长度 > 500
    assert len(pdf_bytes) > 500
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_export.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'uni_rag.export.pdf_exporter'`）

- [ ] **Step 3: 写 src/uni_rag/export/pdf_exporter.py**

```python
"""Render a query payload to PDF bytes using weasyprint."""
from __future__ import annotations
from markdown_it import MarkdownIt
from weasyprint import HTML
from uni_rag.export.md_exporter import render_markdown, ExportPayload


_PDF_CSS = """
@page { size: A4; margin: 2cm; }
body { font-family: 'Noto Serif CJK SC', 'Songti SC', 'STSong', serif;
       line-height: 1.7; color: #1a1a1a; }
h1 { font-size: 1.8rem; border-bottom: 1px solid #d4cfc5; padding-bottom: 0.3em; }
h2 { font-size: 1.2rem; color: #6b4f3a; margin-top: 1.5em; }
blockquote { color: #6b4f3a; font-style: italic; }
pre { background: #f5f1e8; padding: 0.5em; border-left: 3px solid #6b4f3a;
      white-space: pre-wrap; }
code { font-family: 'JetBrains Mono', 'Menlo', monospace; font-size: 0.9em; }
"""


def render_pdf(payload: ExportPayload) -> bytes:
    """Convert a Q&A payload to PDF bytes via Markdown → HTML → PDF."""
    md_text = render_markdown(payload)
    md = MarkdownIt()
    html_body = md.render(md_text)
    html_doc = (
        "<!DOCTYPE html><html><head><meta charset='utf-8'>"
        f"<style>{_PDF_CSS}</style></head><body>{html_body}</body></html>"
    )
    return HTML(string=html_doc).write_pdf()
```

- [ ] **Step 4: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_export.py -v`
Expected: 6 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/export/pdf_exporter.py tests/unit/test_export.py
git commit -m "feat(export): PDF exporter via markdown-it-py + weasyprint"
```

### Task 9.4: API 端点导出 message

**Files:**
- Modify: `src/uni_rag/api/schemas.py`
- Modify: `src/uni_rag/api/routes.py`
- Modify: `tests/integration/test_api.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/integration/test_api.py`:

```python
def test_export_message_markdown(client, tmp_path, monkeypatch):
    """GET .../export?format=md 返回 markdown 文件下载。"""
    def fake_complete(self, system, max_tokens=1024):
        return "answer with [src]"
    monkeypatch.setattr("uni_rag.llm.client.LLMClient.complete", fake_complete)

    pdf = Path(__file__).resolve().parents[1] / "fixtures" / "sample.pdf"
    with open(pdf, "rb") as f:
        r = client.post("/api/ingest", files={"file": ("sample.pdf", f, "application/pdf")})
    assert r.status_code == 200

    # 发一个问题
    r = client.post("/api/query", json={"question": "what is supervised learning?"})
    assert r.status_code == 200

    # 从 SessionStore 拿到 session_id 和 message seq
    from uni_rag.config import load_settings
    from uni_rag.session.store import SessionStore
    sess = SessionStore(load_settings().sessions_db_path)
    # 找任意一个非空 session
    import sqlite3
    with sqlite3.connect(load_settings().sessions_db_path) as conn:
        rows = conn.execute("SELECT DISTINCT session_id FROM messages LIMIT 1").fetchall()
    assert rows
    sid = rows[0][0]
    # 第一个 assistant message 是 seq=1
    r = client.get(f"/api/sessions/{sid}/messages/1/export?format=md")
    assert r.status_code == 200
    assert "text/markdown" in r.headers["content-type"]
    body = r.text
    assert "# 问答记录" in body
    assert "## 问题" in body


def test_export_message_pdf(client, tmp_path, monkeypatch):
    """GET .../export?format=pdf 返回 application/pdf bytes。"""
    def fake_complete(self, system, max_tokens=1024):
        return "answer with [src]"
    monkeypatch.setattr("uni_rag.llm.client.LLMClient.complete", fake_complete)

    pdf = Path(__file__).resolve().parents[1] / "fixtures" / "sample.pdf"
    with open(pdf, "rb") as f:
        client.post("/api/ingest", files={"file": ("sample.pdf", f, "application/pdf")})
    client.post("/api/query", json={"question": "q"})

    from uni_rag.config import load_settings
    import sqlite3
    with sqlite3.connect(load_settings().sessions_db_path) as conn:
        rows = conn.execute("SELECT DISTINCT session_id FROM messages LIMIT 1").fetchall()
    sid = rows[0][0]
    r = client.get(f"/api/sessions/{sid}/messages/1/export?format=pdf")
    assert r.status_code == 200
    assert r.headers["content-type"] == "application/pdf"
    assert r.content.startswith(b"%PDF-")


def test_export_message_invalid_format(client, tmp_path):
    from uni_rag.config import load_settings
    from uni_rag.session.store import SessionStore
    sid = SessionStore(load_settings().sessions_db_path).create()
    SessionStore(load_settings().sessions_db_path).append(sid, "user", "q")
    SessionStore(load_settings().sessions_db_path).append(sid, "assistant", "a")
    r = client.get(f"/api/sessions/{sid}/messages/1/export?format=docx")
    assert r.status_code == 400
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_api.py -v`
Expected: 3 个新 test FAIL (404)

- [ ] **Step 3: 在 schemas.py 加 ExportResponse 占用类型 + format enum**

在 `src/uni_rag/api/schemas.py` 末尾追加:

```python
class ExportFormat(str):
    MD = "md"
    PDF = "pdf"
```

（Pydantic 字符串枚举足够；不在这里用 Enum 避免 FastAPI 复杂度。）

- [ ] **Step 4: 在 routes.py 加 /export 端点**

修改 `src/uni_rag/api/routes.py`，在 import 处追加:

```python
from fastapi.responses import Response
from uni_rag.export.md_exporter import render_markdown
from uni_rag.export.pdf_exporter import render_pdf
```

在文件末尾追加:

```python
def _fetch_message(session_id: str, message_index: int) -> tuple[str, str] | None:
    """Return (role, content) for the Nth message (1-based) in a session, or None."""
    from uni_rag.session.store import SessionStore
    settings = load_settings()
    store = SessionStore(settings.sessions_db_path)
    msgs = store.get(session_id)
    if not msgs:
        return None
    if message_index < 1 or message_index > len(msgs):
        return None
    msg = msgs[message_index - 1]
    return msg["role"], msg["content"]


def _build_export_payload(
    session_id: str,
    message_index: int,
    question: str,
    answer: str,
) -> dict:
    """Build a {question, answer, citations} payload for the export modules.

    Re-runs retrieval on the same question to re-derive citations consistently.
    If the original retrieval would fail (e.g. cleared vector store), citations is [].
    """
    payload: dict = {"question": question, "answer": answer, "citations": []}
    try:
        pipeline = get_pipeline()
        result = pipeline.query(question, session_id=None, top_k=5)
        payload["citations"] = result.get("citations", [])
    except Exception:
        # 导出在主对话失败时仍要可用；citations 留空
        payload["citations"] = []
    return payload


@router.get("/sessions/{session_id}/messages/{message_index}/export")
def export_message(session_id: str, message_index: int, format: str = "md"):
    """Download a single assistant message as Markdown or PDF.

    `format` must be 'md' or 'pdf'. The Nth (1-based) message must be 'assistant';
    we walk back to find the most recent 'user' message to use as the question.
    """
    fmt = (format or "").lower()
    if fmt not in ("md", "pdf"):
        raise HTTPException(400, "format must be 'md' or 'pdf'")

    settings = load_settings()
    from uni_rag.session.store import SessionStore
    store = SessionStore(settings.sessions_db_path)
    msgs = store.get(session_id)
    if not msgs:
        raise HTTPException(404, f"Session not found or empty: {session_id}")
    if message_index < 1 or message_index > len(msgs):
        raise HTTPException(404, f"message_index out of range")

    role, answer = msgs[message_index - 1]["role"], msgs[message_index - 1]["content"]
    if role != "assistant":
        raise HTTPException(400, "Only assistant messages can be exported")

    # 找前一条 user message 作为 question
    question = ""
    for j in range(message_index - 2, -1, -1):
        if msgs[j]["role"] == "user":
            question = msgs[j]["content"]
            break
    if not question:
        question = "（无对应问题）"

    payload = _build_export_payload(session_id, message_index, question, answer)

    if fmt == "md":
        md_text = render_markdown(payload)
        return Response(
            content=md_text,
            media_type="text/markdown; charset=utf-8",
            headers={
                "Content-Disposition": f'attachment; filename="uni-rag-msg-{message_index}.md"'
            },
        )
    # pdf
    pdf_bytes = render_pdf(payload)
    return Response(
        content=pdf_bytes,
        media_type="application/pdf",
        headers={
            "Content-Disposition": f'attachment; filename="uni-rag-msg-{message_index}.pdf"'
        },
    )
```

- [ ] **Step 5: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_api.py -v`
Expected: 6 passed (含 3 个原 + 3 个新)

- [ ] **Step 6: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/api/ tests/integration/test_api.py
git commit -m "feat(api): export single message to md/pdf download"
```

### Task 9.5: 前端 Download 按钮

**Files:**
- Modify: `src/uni_rag/web/app.js`
- Modify: `src/uni_rag/web/style.css`

- [ ] **Step 1: 改 addMessage 在 assistant 消息上挂 Download 按钮**

在 `src/uni_rag/web/app.js` 的 `addMessage` 函数里，`if (citations.length) { ... }` 块后面、`chat.appendChild(div)` 之前，插入:

```javascript
  // Export 按钮（仅 assistant 消息）
  if (role === 'assistant' && sessionId) {
    const actions = document.createElement('div');
    actions.className = 'msg-actions';
    const seq = (window.__msgCounter = (window.__msgCounter || 0) + 1);
    ['md', 'pdf'].forEach((fmt) => {
      const a = document.createElement('a');
      a.className = 'download-link';
      a.textContent = fmt === 'md' ? 'Download .md' : 'Download .pdf';
      a.href = `/api/sessions/${sessionId}/messages/${seq}/export?format=${fmt}`;
      a.setAttribute('download', `uni-rag-msg-${seq}.${fmt}`);
      actions.appendChild(a);
    });
    div.appendChild(actions);
  }
```

- [ ] **Step 2: 在 style.css 加按钮样式**

在 `src/uni_rag/web/style.css` 末尾追加:

```css
.msg-actions { margin-top: 0.5rem; display: flex; gap: 0.5rem; }
.download-link {
  display: inline-block;
  padding: 0.25rem 0.7rem;
  background: var(--soft);
  border: 1px solid var(--border);
  border-radius: 3px;
  font-size: 0.78rem;
  color: var(--accent);
  text-decoration: none;
}
.download-link:hover { background: #e0d8c8; }
```

- [ ] **Step 3: 手动验证**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
uv run uni-rag serve
```

打开 `http://127.0.0.1:8765/`，上传 sample.pdf，问一个问题 → 在 assistant 消息底部看到 "Download .md" / "Download .pdf" 链接 → 点击触发下载，文件内容含问题、答案、引用。

- [ ] **Step 4: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/web/
git commit -m "feat(web): download .md/.pdf links on assistant messages"
```

---

## Milestone 10: Docker 打包 (B9)

**目的:** 一行 `docker compose up -d` 起服务；数据持久化到本机 volume，模型下载到镜像内避免冷启动。

**镜像设计：**
- Base: `python:3.13-slim`（小，约 150MB）
- 系统依赖：build-essential（编译 sentence-transformers / chroma hnsw 用的 lz4/snappy）+ libpango-1.0-0 + libpangoft2-1.0-0（weasyprint PDF 需要）
- 装 `uv` 加速依赖安装
- 复制 `pyproject.toml` + `uv.lock` → `uv sync --no-dev`
- 复制源码 + web 静态
- 下载 BGE-M3 / bge-reranker-base 到 `/root/.cache/huggingface/`（构建期做，避免运行时冷启动）
- 暴露 8766
- CMD: `uvicorn uni_rag.api.app:create_app --factory --host 0.0.0.0 --port 8766`

### Task 10.1: Dockerfile

**Files:**
- Create: `Dockerfile`

- [ ] **Step 1: 写 Dockerfile**

```dockerfile
# syntax=docker/dockerfile:1.6
FROM python:3.13-slim AS base

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1 \
    UV_LINK_MODE=copy \
    UV_PROJECT_ENVIRONMENT=/usr/local

# System deps: build tools for hnsw/lz4/snappy, libpango for weasyprint
RUN apt-get update && apt-get install -y --no-install-recommends \
        build-essential \
        libpango-1.0-0 \
        libpangoft2-1.0-0 \
        curl \
        ca-certificates \
    && rm -rf /var/lib/apt/lists/*

# Install uv (single binary)
COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv
RUN chmod +x /usr/local/bin/uv

WORKDIR /app

# Copy dependency manifests first for layer caching
COPY pyproject.toml uv.lock ./
RUN uv sync --no-dev --no-install-project

# Copy source
COPY src/ ./src/
COPY cli/ ./cli/
RUN uv sync --no-dev

# Pre-download models at build time so first request isn't slow
# Uses a small Python one-liner; models are cached in /root/.cache/huggingface
RUN uv run python -c "\
from sentence_transformers import SentenceTransformer, CrossEncoder; \
SentenceTransformer('BAAI/bge-m3'); \
CrossEncoder('BAAI/bge-reranker-base')"

# Static web files
COPY src/uni_rag/web/ /app/web/

EXPOSE 8766

# Data lives in /data (mount a volume here for persistence)
ENV UNI_RAG_DATA_DIR=/data \
    UNI_RAG_HOST=0.0.0.0 \
    UNI_RAG_PORT=8766

CMD ["uv", "run", "uvicorn", "uni_rag.api.app:create_app", \
     "--factory", "--host", "0.0.0.0", "--port", "8766"]
```

- [ ] **Step 2: 写 .dockerignore**

```gitignore
.venv/
data/
.git/
.gitignore
.env
.env.example
tests/
__pycache__/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/
node_modules/
.vite/
web/dist/
.DS_Store
docs/
*.md
!README.md
scripts/
```

- [ ] **Step 3: 写 docker-compose.yml**

```yaml
services:
  uni-rag:
    build: .
    image: uni-rag:0.3
    container_name: uni-rag
    restart: unless-stopped
    ports:
      - "8766:8766"
    volumes:
      - uni-rag-data:/data
    env_file:
      - .env

volumes:
  uni-rag-data:
    driver: local
```

- [ ] **Step 4: 在 README.md 加 Docker 段**

修改 `/Users/mahaoxuan/Desktop/产品项目学习/uni-rag/README.md`（如果文件不存在就 `touch`），追加到末尾:

```markdown
## Docker

```bash
# 1) 复制环境变量样板并填 ANTHROPIC_API_KEY
cp .env.example .env
# 编辑 .env

# 2) 一键起服务（首跑会构建镜像，~5-10 分钟下载模型）
docker compose up -d

# 3) 看日志
docker compose logs -f

# 4) 停服务
docker compose down
```

访问 `http://127.0.0.1:8766/`。

数据持久化到 named volume `uni-rag-data`；删除容器不丢数据。
```

- [ ] **Step 5: 本地构建验证（dry build smoke test）**

注：完整 `docker build` 会下载 ~2GB BGE-M3 + bge-reranker 模型，约 5-10 分钟。任务里先 commit 文件，验证放到 Task 10.2。

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add Dockerfile .dockerignore docker-compose.yml README.md
git commit -m "feat(docker): Dockerfile + compose for one-command deployment"
```

- [ ] **Step 6: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add Dockerfile .dockerignore docker-compose.yml README.md
git commit -m "feat(docker): Dockerfile + compose for one-command deployment"
```

### Task 10.2: Docker 构建验证（test the build）

**Files:**
- Create: `tests/integration/test_docker_build.py`

- [ ] **Step 1: 写验证脚本（pytest skip when no docker）**

```python
# tests/integration/test_docker_build.py
"""Verify the Docker image builds cleanly. Skip when docker is unavailable."""
import os
import shutil
import subprocess
import pytest


DOCKER_AVAILABLE = shutil.which("docker") is not None


@pytest.mark.skipif(not DOCKER_AVAILABLE, reason="docker not installed in this env")
def test_docker_build_succeeds():
    """Run `docker build` and assert exit code 0. Tagged for caching across runs."""
    result = subprocess.run(
        ["docker", "build", "-t", "uni-rag:test-v0.3", "."],
        cwd=os.path.dirname(os.path.dirname(os.path.dirname(__file__))),
        capture_output=True,
        text=True,
        timeout=900,  # 15 min — model download is the slow part
    )
    assert result.returncode == 0, (
        f"docker build failed:\nSTDOUT:\n{result.stdout}\nSTDERR:\n{result.stderr}"
    )


@pytest.mark.skipif(not DOCKER_AVAILABLE, reason="docker not installed in this env")
def test_docker_image_has_entrypoint():
    """After build, image should be runnable with --help-style no-op invocation."""
    result = subprocess.run(
        ["docker", "image", "inspect", "uni-rag:test-v0.3"],
        capture_output=True,
        text=True,
    )
    assert result.returncode == 0
    import json
    info = json.loads(result.stdout)
    assert info[0]["Config"]["ExposedPorts"].get("8766/tcp") is not None
```

- [ ] **Step 2: 跑测试**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_docker_build.py -v`
Expected:
- 本机有 docker：2 passed（首次跑需要 5-10 分钟下载模型）
- 本机无 docker：2 skipped

- [ ] **Step 3: 手动 smoke test（仅在本机有 docker 时）**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
docker compose up -d
sleep 5
curl http://127.0.0.1:8766/api/health
# 期望: {"status":"ok"}
docker compose logs --tail=20
docker compose down
```

- [ ] **Step 4: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add tests/integration/test_docker_build.py
git commit -m "test(docker): verify image builds and exposes port 8766"
```

### Task 10.3: GitHub Actions workflow（optional bonus）

**Files:**
- Create: `.github/workflows/docker.yml`

- [ ] **Step 1: 写 workflow**

```yaml
name: docker-build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build
        uses: docker/build-push-action@v5
        with:
          context: .
          tags: uni-rag:ci-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

  # 后续接 ghcr.io push：需要时再开 jobs.push
```

- [ ] **Step 2: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add .github/
git commit -m "ci(github-actions): docker build on push/PR (linux/amd64+arm64)"
```

---

## Milestone 11: 跨 session 知识库 (B10)

**目的:** 多个隔离的"知识库"（KB），每个 KB 是独立的 Chroma collection + BM25 目录。Session 可以绑定 0 或多个 KB，查询时只检索绑定的 KB。v0.2 的"单全局 KB"模式对用户透明地变成"default KB"。

**数据布局：**
```
data/
├── chroma/                  # v0.2 旧全局 collection（迁移期间保留）
│   └── chunks/              # Chroma collection name = "chunks"
├── bm25/
│   └── docs.pkl             # v0.2 旧全局 BM25
├── kb_<id>/
│   ├── chroma/              # 新 KB 的 chroma collection name = "kb_<id>"
│   ├── bm25/
│   │   └── docs.pkl
│   └── uploads/             # 这个 KB 专属的源文件副本
└── kbs.db                   # KB + session_kbs 元数据
```

**KB ID 规则：** `^[a-z0-9_]{3,32}$`，slug 风格（如 `cs101`、`kaoyan`）。Collection name 用 `kb_<id>` 避免与 v0.2 collection 重名。

**默认 KB：** v0.3 启动时如果 `kbs.db` 不存在，自动创建 `default` KB，ID = `default`。v0.2 旧 Chroma collection（`chunks`）+ BM25 不动，访问 default KB 时直接走旧路径；新建 KB 走新路径。

### Task 11.1: KBStore (SQLite)

**Files:**
- Create: `src/uni_rag/store/__init__.py` （可能已存在）
- Create: `src/uni_rag/store/kb.py`
- Create: `tests/unit/test_kb.py`

- [ ] **Step 1: 写失败的测试**

```python
# tests/unit/test_kb.py
import pytest
from uni_rag.store.kb import KBStore


@pytest.fixture
def store(tmp_path):
    return KBStore(tmp_path / "kbs.db")


def test_kbstore_create_and_get(store):
    kb = store.create("CS101", "课程笔记")
    assert kb["id"] == "cs101"
    assert kb["name"] == "CS101"
    assert kb["description"] == "课程笔记"
    assert "created_at" in kb
    assert store.get("cs101") == kb


def test_kbstore_list(store):
    store.create("CS101", "课程笔记")
    store.create("KAOYAN", "考研真题")
    kbs = store.list()
    ids = {k["id"] for k in kbs}
    assert ids == {"cs101", "kaoyan"}


def test_kbstore_duplicate_id_raises(store):
    store.create("CS101", "x")
    with pytest.raises(ValueError, match="exists"):
        store.create("CS101", "y")


def test_kbstore_invalid_id_raises(store):
    with pytest.raises(ValueError, match="invalid"):
        store.create("AB", "bad")  # 太短
    with pytest.raises(ValueError, match="invalid"):
        store.create("CS 101", "bad")  # 含空格
    with pytest.raises(ValueError, match="invalid"):
        store.create("CS-101", "bad")  # 含连字符


def test_kbstore_delete(store):
    store.create("CS101", "x")
    assert store.delete("cs101") is True
    assert store.get("cs101") is None
    assert store.delete("cs101") is False


def test_kbstore_ensure_default_creates_if_missing(tmp_path):
    db_path = tmp_path / "kbs.db"
    store = KBStore(db_path)
    default = store.ensure_default()
    assert default["id"] == "default"
    # 再次调用幂等
    default2 = store.ensure_default()
    assert default2["id"] == "default"
    assert default["created_at"] == default2["created_at"]


def test_kbstore_session_binding(store):
    store.create("CS101", "x")
    store.create("KAOYAN", "y")
    store.bind_session("sess-1", ["cs101", "kaoyan"])
    bound = store.get_session_kbs("sess-1")
    assert {k["id"] for k in bound} == {"cs101", "kaoyan"}


def test_kbstore_session_binding_empty(store):
    store.bind_session("sess-2", [])
    assert store.get_session_kbs("sess-2") == []


def test_kbstore_session_binding_rejects_unknown_kb(store):
    with pytest.raises(ValueError, match="not found"):
        store.bind_session("sess-3", ["nonexistent"])
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_kb.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'uni_rag.store.kb'`）

- [ ] **Step 3: 写 src/uni_rag/store/kb.py**

```python
"""SQLite-backed store for knowledge bases (KBs) and session↔KB bindings."""
from __future__ import annotations
import re
import sqlite3
import time
from pathlib import Path
from typing import TypedDict


class KbRecord(TypedDict):
    id: str
    name: str
    description: str
    created_at: str  # ISO 8601


_ID_RE = re.compile(r"^[a-z0-9_]{3,32}$")


def _slugify(s: str) -> str:
    s = s.strip().lower()
    s = re.sub(r"[^a-z0-9_]+", "_", s)
    s = re.sub(r"_+", "_", s).strip("_")
    if len(s) < 3:
        s = (s + "___")[:3]
    return s[:32]


def _now() -> str:
    return time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime())


def _validate_id(kb_id: str) -> None:
    if not _ID_RE.match(kb_id):
        raise ValueError(
            f"invalid kb id {kb_id!r}: must match {_ID_RE.pattern} (3-32 chars, a-z0-9_)"
        )


class KBStore:
    """CRUD for KBs + session↔KB associations."""

    def __init__(self, db_path: Path):
        self.db_path = Path(db_path)
        self.db_path.parent.mkdir(parents=True, exist_ok=True)
        self._init_db()

    def _init_db(self) -> None:
        with sqlite3.connect(self.db_path) as conn:
            conn.execute("""
                CREATE TABLE IF NOT EXISTS knowledge_bases (
                    id TEXT PRIMARY KEY,
                    name TEXT NOT NULL,
                    description TEXT NOT NULL DEFAULT '',
                    created_at TEXT NOT NULL
                )
            """)
            conn.execute("""
                CREATE TABLE IF NOT EXISTS session_kbs (
                    session_id TEXT NOT NULL,
                    kb_id TEXT NOT NULL,
                    PRIMARY KEY (session_id, kb_id),
                    FOREIGN KEY (kb_id) REFERENCES knowledge_bases(id) ON DELETE CASCADE
                )
            """)
            conn.execute("""
                CREATE INDEX IF NOT EXISTS idx_session_kbs_session
                ON session_kbs(session_id)
            """)

    # --- KB CRUD ---

    def create(self, name: str, description: str = "", kb_id: str | None = None) -> KbRecord:
        """Create a KB. kb_id auto-derived from name if not given. Raises if id exists/invalid."""
        if kb_id is None:
            kb_id = _slugify(name)
        _validate_id(kb_id)
        now = _now()
        with sqlite3.connect(self.db_path) as conn:
            try:
                conn.execute(
                    "INSERT INTO knowledge_bases (id, name, description, created_at) "
                    "VALUES (?, ?, ?, ?)",
                    (kb_id, name, description, now),
                )
            except sqlite3.IntegrityError as e:
                raise ValueError(f"kb {kb_id!r} already exists") from e
        return KbRecord(id=kb_id, name=name, description=description, created_at=now)

    def get(self, kb_id: str) -> KbRecord | None:
        with sqlite3.connect(self.db_path) as conn:
            row = conn.execute(
                "SELECT id, name, description, created_at FROM knowledge_bases WHERE id = ?",
                (kb_id,),
            ).fetchone()
        if not row:
            return None
        return KbRecord(id=row[0], name=row[1], description=row[2], created_at=row[3])

    def list(self) -> list[KbRecord]:
        with sqlite3.connect(self.db_path) as conn:
            rows = conn.execute(
                "SELECT id, name, description, created_at FROM knowledge_bases ORDER BY created_at"
            ).fetchall()
        return [
            KbRecord(id=r[0], name=r[1], description=r[2], created_at=r[3]) for r in rows
        ]

    def delete(self, kb_id: str) -> bool:
        with sqlite3.connect(self.db_path) as conn:
            cur = conn.execute("DELETE FROM knowledge_bases WHERE id = ?", (kb_id,))
            return cur.rowcount > 0

    def ensure_default(self) -> KbRecord:
        """Return the 'default' KB, creating it if missing. Idempotent."""
        existing = self.get("default")
        if existing:
            return existing
        return self.create("default", "默认知识库（v0.2 兼容）", kb_id="default")

    # --- session binding ---

    def bind_session(self, session_id: str, kb_ids: list[str]) -> None:
        """Replace the session's KB binding with the given list. Empty list = unbind."""
        # 验证所有 KB 都存在
        for kid in kb_ids:
            if self.get(kid) is None:
                raise ValueError(f"kb {kid!r} not found")
        with sqlite3.connect(self.db_path) as conn:
            conn.execute("DELETE FROM session_kbs WHERE session_id = ?", (session_id,))
            for kid in kb_ids:
                conn.execute(
                    "INSERT INTO session_kbs (session_id, kb_id) VALUES (?, ?)",
                    (session_id, kid),
                )

    def get_session_kbs(self, session_id: str) -> list[KbRecord]:
        with sqlite3.connect(self.db_path) as conn:
            rows = conn.execute(
                """
                SELECT kb.id, kb.name, kb.description, kb.created_at
                FROM session_kbs sk
                JOIN knowledge_bases kb ON kb.id = sk.kb_id
                WHERE sk.session_id = ?
                ORDER BY kb.created_at
                """,
                (session_id,),
            ).fetchall()
        return [
            KbRecord(id=r[0], name=r[1], description=r[2], created_at=r[3]) for r in rows
        ]
```

- [ ] **Step 4: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_kb.py -v`
Expected: 9 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/store/kb.py tests/unit/test_kb.py
git commit -m "feat(kb): KBStore CRUD + session binding + ensure_default"
```

### Task 11.2: Settings 加 kb_db_path

**Files:**
- Modify: `src/uni_rag/config.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/unit/test_config.py`:

```python
def test_kb_db_path_default():
    from uni_rag.config import load_settings
    s = load_settings()
    assert s.kb_db_path == s.data_dir / "kbs.db"
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_config.py -v`
Expected: FAIL

- [ ] **Step 3: 在 Settings 加字段**

修改 `src/uni_rag/config.py` 的 `Settings` 类，在 `sessions_db_path` 之后追加:

```python
    @property
    def kb_db_path(self) -> Path:
        return self.data_dir / "kbs.db"

    @property
    def kb_dir(self) -> Path:
        """Base dir for per-KB subdirectories (chroma/, bm25/, uploads/)."""
        p = self.data_dir / "kbs"
        p.mkdir(parents=True, exist_ok=True)
        return p
```

- [ ] **Step 4: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_config.py -v`
Expected: passed

- [ ] **Step 5: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/config.py tests/unit/test_config.py
git commit -m "feat(config): kb_db_path + kb_dir properties"
```

### Task 11.3: VectorStore + BM25Index 接受 collection_name / index_dir

**Files:**
- Modify: `src/uni_rag/store/vector.py`
- Modify: `tests/unit/test_vector.py`

- [ ] **Step 1: 改 VectorStore 接受可选 collection_name**

完全替换 `src/uni_rag/store/vector.py`（注意保持 `collection` 公共属性以兼容 v0.1/v0.2 测试和 BDD）:

```python
"""Chroma wrapper for vector storage. Each KB = one collection."""
from __future__ import annotations
from pathlib import Path
import chromadb
from chromadb.config import Settings as ChromaSettings
from uni_rag.config import load_settings


class VectorStore:
    """Persistent Chroma collection wrapper.

    `data_dir` = base directory; `collection_name` = unique per KB.
    v0.2 default collection name was 'chunks'; v0.3 KB collections are 'kb_<id>'.
    """

    def __init__(
        self,
        data_dir: Path | None = None,
        collection_name: str = "chunks",
    ):
        if data_dir is None:
            data_dir = load_settings().chroma_dir
        self.client = chromadb.PersistentClient(
            path=str(data_dir),
            settings=ChromaSettings(anonymized_telemetry=False),
        )
        self.collection = self.client.get_or_create_collection(
            name=collection_name,
            metadata={"hnsw:space": "cosine"},
        )

    def add(
        self,
        source_id: str,
        chunk_id: str,
        embedding: list[float],
        metadata: dict,
        document: str | None = None,
    ) -> None:
        self.collection.add(
            ids=[chunk_id],
            embeddings=[embedding],
            metadatas=[metadata],
            documents=[document] if document else None,
        )

    def query(
        self,
        embedding: list[float],
        top_k: int = 5,
        where: dict | None = None,
    ) -> list[dict]:
        res = self.collection.query(
            query_embeddings=[embedding],
            n_results=top_k,
            where=where,
        )
        out = []
        for i, chunk_id in enumerate(res["ids"][0]):
            out.append({
                "id": chunk_id,
                "score": 1 - res["distances"][0][i],  # cosine distance → similarity
                "metadata": res["metadatas"][0][i] if res["metadatas"] else {},
                "document": res["documents"][0][i] if res["documents"] else None,
            })
        return out
```

> **设计说明：** v0.1/v0.2 默认 collection name `"chunks"` 保留 — `VectorStore()` 不传参时行为完全不变。M11 的 `IngestPipeline(kb_id=...)` 才会传 `kb_<id>`。

- [ ] **Step 2: 跑现有测试通过（无回归）**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_vector.py tests/unit/test_bm25.py -v`
Expected: 全部 passed（参数是 optional，向后兼容）

- [ ] **Step 3: 写多 KB 隔离测试**

追加到 `tests/unit/test_vector.py`:

```python
def test_vector_store_isolates_collections(tmp_path, monkeypatch):
    """两个 collection 互不污染。"""
    monkeypatch.setenv("UNI_RAG_DATA_DIR", str(tmp_path))
    a = VectorStore(collection_name="kb_a")
    b = VectorStore(collection_name="kb_b")
    a.add("src", "c1", [0.1] * 1024, {"source": "a.txt"}, document="alpha content")
    b.add("src", "c2", [0.2] * 1024, {"source": "b.txt"}, document="beta content")
    # A 查不到 B 的 chunk
    a_results = a.query([0.1] * 1024, top_k=5)
    assert all(r["metadata"].get("source") == "a.txt" for r in a_results)
    b_results = b.query([0.2] * 1024, top_k=5)
    assert all(r["metadata"].get("source") == "b.txt" for r in b_results)
```

- [ ] **Step 4: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_vector.py -v`
Expected: 全部 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/store/vector.py tests/unit/test_vector.py
git commit -m "feat(vector): VectorStore accepts collection_name (per-KB isolation)"
```

### Task 11.4: IngestPipeline + HybridRetriever + RAGPipeline 支持 kb_id

**Files:**
- Modify: `src/uni_rag/ingest/pipeline.py`
- Modify: `src/uni_rag/retrieve/retriever.py`
- Modify: `src/uni_rag/rag/pipeline.py`
- Modify: `tests/integration/test_query_pipeline.py`

- [ ] **Step 1: 改 IngestPipeline 接受 kb_id**

完全替换 `src/uni_rag/ingest/pipeline.py`:

```python
"""Ingest pipeline: parse → chunk → embed → store. Supports per-KB isolation."""
from __future__ import annotations
import hashlib
from pathlib import Path
from uni_rag.ingest.parsers import parse_document
from uni_rag.ingest.chunker import chunk_document
from uni_rag.ingest.embedder import get_embedder
from uni_rag.store.vector import VectorStore
from uni_rag.store.bm25 import BM25Index
from uni_rag.config import load_settings


class IngestPipeline:
    """kb_id=None = legacy single-KB mode (v0.2 default collection 'chunks', single BM25 dir)."""

    def __init__(self, kb_id: str | None = None):
        self.embedder = get_embedder()
        self.kb_id = kb_id
        if kb_id is None:
            # Legacy: v0.2 兼容模式
            self.vector = VectorStore()
            self.bm25 = BM25Index(load_settings().bm25_dir)
            self.uploads_dir = load_settings().uploads_dir
        else:
            # Per-KB mode
            data_dir = load_settings().data_dir
            kb_base = data_dir / "kbs" / kb_id
            chroma_dir = kb_base / "chroma"
            bm25_dir = kb_base / "bm25"
            uploads_dir = kb_base / "uploads"
            chroma_dir.mkdir(parents=True, exist_ok=True)
            bm25_dir.mkdir(parents=True, exist_ok=True)
            uploads_dir.mkdir(parents=True, exist_ok=True)
            self.vector = VectorStore(data_dir=chroma_dir, collection_name=f"kb_{kb_id}")
            self.bm25 = BM25Index(bm25_dir)
            self.uploads_dir = uploads_dir

    def _source_id(self, path: Path) -> str:
        h = hashlib.sha256()
        h.update(str(path.resolve()).encode())
        h.update(path.read_bytes()[:1024 * 1024])  # 前 1MB
        return h.hexdigest()[:16]

    def ingest_file(self, path: Path, original_name: str | None = None) -> dict:
        path = Path(path)
        save_name = original_name or path.name
        dest = self.uploads_dir / save_name
        dest.write_bytes(path.read_bytes())

        doc = parse_document(dest)
        source_id = self._source_id(dest)

        chunks = chunk_document(doc.text, source_id=source_id)
        if not chunks:
            return {"source_id": source_id, "chunks": 0, "format": doc.format}

        texts = [c.text for c in chunks]
        vecs = self.embedder.embed(texts)

        for c, v in zip(chunks, vecs):
            self.vector.add(
                source_id=source_id,
                chunk_id=f"{source_id}:{c.start_offset}",
                embedding=v,
                metadata={
                    "source": save_name,
                    "format": doc.format,
                    "section": c.section_title or "",
                    "start": c.start_offset,
                    "end": c.end_offset,
                },
                document=c.text,
            )

        for c in chunks:
            self.bm25.add(
                chunk_id=f"{source_id}:{c.start_offset}",
                text=c.text,
                metadata={"source": save_name, "section": c.section_title or ""},
            )
        self.bm25.save()

        return {"source_id": source_id, "chunks": len(chunks), "format": doc.format}

    def search(self, query: str, top_k: int = 5) -> list[dict]:
        """Test seam: vector-only search within this KB."""
        vec = self.embedder.embed([query])[0]
        return self.vector.query(vec, top_k=top_k)
```

- [ ] **Step 2: 改 HybridRetriever 接受 kb_id**

完全替换 `src/uni_rag/retrieve/retriever.py`:

```python
"""Hybrid retriever: vector + BM25 + rerank. Per-KB isolation via kb_id."""
from __future__ import annotations
from uni_rag.ingest.embedder import get_embedder
from uni_rag.store.vector import VectorStore
from uni_rag.store.bm25 import BM25Index
from uni_rag.retrieve.reranker import Reranker
from uni_rag.config import load_settings


class HybridRetriever:
    """kb_id=None = legacy v0.2 mode (single global collection + BM25 dir)."""

    def __init__(self, kb_id: str | None = None):
        self.embedder = get_embedder()
        self.kb_id = kb_id
        if kb_id is None:
            self.vector = VectorStore()
            self.bm25 = BM25Index.load(load_settings().bm25_dir)
        else:
            data_dir = load_settings().data_dir
            kb_base = data_dir / "kbs" / kb_id
            chroma_dir = kb_base / "chroma"
            bm25_dir = kb_base / "bm25"
            self.vector = VectorStore(data_dir=chroma_dir, collection_name=f"kb_{kb_id}")
            self.bm25 = BM25Index.load(bm25_dir)
        self.reranker = Reranker()

    def retrieve(
        self,
        query: str,
        top_k: int = 5,
        source_filter: str | None = None,
    ) -> list[dict]:
        where = {"source": source_filter} if source_filter else None
        vec = self.embedder.embed([query])[0]
        vec_results = self.vector.query(vec, top_k=top_k * 3, where=where)
        bm25_results = self.bm25.query(query, top_k=top_k * 3)
        if source_filter:
            bm25_results = [r for r in bm25_results if r["metadata"].get("source") == source_filter]
        merged: dict[str, dict] = {}
        for r in vec_results:
            merged[r["id"]] = r
        for r in bm25_results:
            if r["id"] not in merged:
                merged[r["id"]] = r
        docs = list(merged.values())
        return self.reranker.rerank(query, docs, top_k=top_k)
```

- [ ] **Step 3: 改 RAGPipeline 接受 kb_id**

修改 `src/uni_rag/rag/pipeline.py` 的 `__init__` 和 `ingest_file` / `query` 签名 + 实现。

完全替换 `src/uni_rag/rag/pipeline.py`:

```python
"""RAG pipeline: query → retrieve → LLM → citations. Supports per-KB isolation."""
from __future__ import annotations
import re
from pathlib import Path
from uni_rag.ingest.pipeline import IngestPipeline
from uni_rag.retrieve.retriever import HybridRetriever
from uni_rag.llm.client import LLMClient
from uni_rag.llm.prompts import SYSTEM_PROMPT, build_user_prompt
from uni_rag.cite.locator import locate_citation
from uni_rag.config import load_settings


_CITE_RE = re.compile(r"\[([a-zA-Z0-9_]+:\d+)\]")


class RAGPipeline:
    """kb_id=None = legacy v0.2 mode (single global KB)."""

    def __init__(self, kb_id: str | None = None):
        from uni_rag.session.store import SessionStore
        self.kb_id = kb_id
        self.ingest = IngestPipeline(kb_id=kb_id)
        self.retriever = HybridRetriever(kb_id=kb_id)
        self.llm = LLMClient()
        # uploads_dir 用于回查原文做 span 定位
        self.uploads_dir = (
            self.ingest.uploads_dir  # IngestPipeline 已经按 kb_id 算好
        )
        self.session_store = SessionStore(load_settings().sessions_db_path)

    def ingest_file(self, path: Path, original_name: str | None = None) -> dict:
        return self.ingest.ingest_file(path, original_name=original_name)

    def query(
        self,
        question: str,
        session_id: str | None = None,
        top_k: int = 5,
    ) -> dict:
        # 1. 检索（KB-scoped）
        chunks = self.retriever.retrieve(question, top_k=top_k)

        # 2. 加载历史
        history = []
        if session_id:
            settings = load_settings()
            cap = settings.max_session_messages
            history = self.session_store.get_recent(session_id, max(cap - 1, 0))

        # 3. 构造 prompt
        self.llm.clear_messages()
        for m in history:
            if m["role"] == "user":
                self.llm.add_user_message(m["content"])
            elif m["role"] == "assistant":
                self.llm.add_assistant_message(m["content"])

        if not chunks:
            answer = "未找到相关资料。请尝试换个问法，或先上传相关文档。"
        else:
            user_prompt = build_user_prompt(question, chunks)
            self.llm.add_user_message(user_prompt)
            answer = self.llm.complete(SYSTEM_PROMPT)

        citations = self._extract_citations(answer, chunks)

        if session_id:
            self.session_store.append(session_id, "user", question)
            self.session_store.append(session_id, "assistant", answer)

        return {
            "answer": answer,
            "citations": citations,
            "chunks_used": chunks,
        }

    def _extract_citations(self, answer: str, chunks: list[dict]) -> list[dict]:
        chunk_map = {c["id"]: c for c in chunks}
        seen = set()
        out = []
        for m in _CITE_RE.finditer(answer):
            cid = m.group(1)
            if cid in seen:
                continue
            seen.add(cid)
            chunk = chunk_map.get(cid)
            if chunk is not None:
                meta = chunk.get("metadata", {})
                src = meta.get("source", "")
                section = meta.get("section", "")
                cited_text = chunk.get("document") or ""
                src_path = self.uploads_dir / src
                span = None
                if src_path.exists():
                    full = src_path.read_text(errors="ignore")
                    _, span = locate_citation(full, cited_text)
            else:
                meta = {}
                src = ""
                section = ""
                cited_text = ""
                span = None
            out.append({
                "chunk_id": cid,
                "source": src,
                "section": section,
                "text": cited_text,
                "span": span,
            })
        return out
```

- [ ] **Step 4: 跑现有测试无回归**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_query_pipeline.py -v`
Expected: 全部 passed（kb_id 默认 None，行为与 v0.2 一致）

- [ ] **Step 5: 写多 KB 隔离测试**

追加到 `tests/integration/test_query_pipeline.py`:

```python
def test_multi_kb_isolation(tmp_path, monkeypatch):
    """上传到 KB A 的内容，不应在 KB B 的检索中出现。"""
    monkeypatch.setenv("UNI_RAG_DATA_DIR", str(tmp_path))
    monkeypatch.setenv("ANTHROPIC_API_KEY", "test-key")

    from uni_rag.store.kb import KBStore
    kb_store = KBStore(tmp_path / "kbs.db")
    kb_store.create("alpha", "alpha 课", kb_id="alpha")
    kb_store.create("beta", "beta 课", kb_id="beta")

    # 准备两份 fixture
    pdf = Path(__file__).resolve().parents[1] / "fixtures" / "sample.pdf"
    other = tmp_path / "beta.pdf"
    other.write_bytes(pdf.read_bytes())

    # 入库到两个 KB
    p_alpha = RAGPipeline(kb_id="alpha")
    p_alpha.ingest_file(pdf, original_name="alpha.pdf")
    p_beta = RAGPipeline(kb_id="beta")
    p_beta.ingest_file(other, original_name="beta.pdf")

    # alpha pipeline 检索，只能看到 alpha
    a_results = p_alpha.retriever.retrieve("supervised learning", top_k=10)
    assert all(r["metadata"].get("source") == "alpha.pdf" for r in a_results)

    # beta pipeline 检索，只能看到 beta
    b_results = p_beta.retriever.retrieve("supervised learning", top_k=10)
    assert all(r["metadata"].get("source") == "beta.pdf" for r in b_results)
```

- [ ] **Step 6: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_query_pipeline.py -v`
Expected: 全部 passed

- [ ] **Step 7: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/ingest/pipeline.py src/uni_rag/retrieve/retriever.py src/uni_rag/rag/pipeline.py tests/integration/test_query_pipeline.py
git commit -m "feat(kb): IngestPipeline/Retriever/RAGPipeline accept kb_id"
```

### Task 11.5: SessionStore 加 session_kbs 关联（可选 — KBStore 已处理）

**说明：** `KBStore` 已经独立管理 `session_kbs` 表（Task 11.1）。SessionStore 不需要改。Session 的 KB 绑定走 `KBStore.bind_session` / `get_session_kbs`。

如果未来想把 session message 和 KB 绑在同一行，可用外键。当前实现两个表分离即可。

### Task 11.6: API 路由 — KB CRUD + session-KB 绑定

**Files:**
- Modify: `src/uni_rag/api/schemas.py`
- Modify: `src/uni_rag/api/routes.py`
- Modify: `tests/integration/test_api.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/integration/test_api.py`:

```python
def test_kb_crud_via_api(client):
    """POST /api/kbs 创建，GET /api/kbs 列出，GET /api/kbs/{id} 详情，DELETE 删除。"""
    r = client.post("/api/kbs", json={"name": "CS101", "description": "课程笔记"})
    assert r.status_code == 200, r.text
    kb = r.json()
    assert kb["id"] == "cs101"
    assert kb["name"] == "CS101"

    r = client.get("/api/kbs")
    assert r.status_code == 200
    kbs = r.json()["kbs"]
    assert any(k["id"] == "cs101" for k in kbs)

    r = client.get("/api/kbs/cs101")
    assert r.status_code == 200
    assert r.json()["id"] == "cs101"

    r = client.delete("/api/kbs/cs101")
    assert r.status_code == 200
    assert r.json()["deleted"] is True

    r = client.get("/api/kbs/cs101")
    assert r.status_code == 404


def test_kb_ingest_uses_kb_scoped_collection(client, tmp_path, monkeypatch):
    """POST /api/kbs/{id}/ingest 入库到该 KB 的 collection。"""
    monkeypatch.setattr("uni_rag.llm.client.LLMClient.complete",
                        lambda *a, **kw: "ok")
    r = client.post("/api/kbs", json={"name": "CS101"})
    assert r.status_code == 200

    pdf = Path(__file__).resolve().parents[1] / "fixtures" / "sample.pdf"
    with open(pdf, "rb") as f:
        r = client.post(
            "/api/kbs/cs101/ingest",
            files={"file": ("sample.pdf", f, "application/pdf")},
        )
    assert r.status_code == 200, r.text
    assert r.json()["chunks"] > 0

    # 验证文件落到了 per-KB uploads
    from uni_rag.config import load_settings
    kb_uploads = load_settings().data_dir / "kbs" / "cs101" / "uploads"
    assert (kb_uploads / "sample.pdf").exists()


def test_session_kb_binding_via_api(client):
    r = client.post("/api/kbs", json={"name": "A"})
    r = client.post("/api/kbs", json={"name": "B"})

    sid = "test-sess-1"
    r = client.post(
        f"/api/sessions/{sid}/kbs",
        json={"kb_ids": ["a", "b"]},
    )
    assert r.status_code == 200, r.text

    r = client.get(f"/api/sessions/{sid}/kbs")
    assert r.status_code == 200
    bound = r.json()["kbs"]
    assert {k["id"] for k in bound} == {"a", "b"}
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_api.py -v`
Expected: 3 个新 test FAIL (404)

- [ ] **Step 3: 在 schemas.py 加 schemas**

在 `src/uni_rag/api/schemas.py` 末尾追加:

```python
class KbCreateRequest(BaseModel):
    name: str
    description: str = ""
    id: str | None = None  # 可选指定 ID


class KbInfo(BaseModel):
    id: str
    name: str
    description: str
    created_at: str


class KbListResponse(BaseModel):
    kbs: list[KbInfo]


class SessionKbBindRequest(BaseModel):
    kb_ids: list[str]


class SessionKbListResponse(BaseModel):
    session_id: str
    kbs: list[KbInfo]


class DeleteResponse(BaseModel):
    deleted: bool
```

- [ ] **Step 4: 在 routes.py 加路由**

修改 `src/uni_rag/api/routes.py` 的 import 处追加:

```python
from uni_rag.store.kb import KBStore
from uni_rag.rag.pipeline import RAGPipeline as _RAGPipelineClass
from uni_rag.api.schemas import (
    KbCreateRequest, KbInfo, KbListResponse,
    SessionKbBindRequest, SessionKbListResponse,
    DeleteResponse,
)
```

在文件末尾追加:

```python
def _get_kb_store() -> KBStore:
    return KBStore(load_settings().kb_db_path)


def _kb_to_info(kb: dict) -> KbInfo:
    return KbInfo(
        id=kb["id"],
        name=kb["name"],
        description=kb.get("description", ""),
        created_at=kb.get("created_at", ""),
    )


@router.post("/kbs", response_model=KbInfo)
def create_kb(req: KbCreateRequest):
    store = _get_kb_store()
    try:
        kb = store.create(req.name, req.description, kb_id=req.id)
    except ValueError as e:
        raise HTTPException(400, str(e))
    return _kb_to_info(kb)


@router.get("/kbs", response_model=KbListResponse)
def list_kbs():
    return KbListResponse(kbs=[_kb_to_info(k) for k in _get_kb_store().list()])


@router.get("/kbs/{kb_id}", response_model=KbInfo)
def get_kb(kb_id: str):
    kb = _get_kb_store().get(kb_id)
    if kb is None:
        raise HTTPException(404, f"kb {kb_id!r} not found")
    return _kb_to_info(kb)


@router.delete("/kbs/{kb_id}", response_model=DeleteResponse)
def delete_kb(kb_id: str):
    deleted = _get_kb_store().delete(kb_id)
    return DeleteResponse(deleted=deleted)


@router.post("/kbs/{kb_id}/ingest", response_model=IngestResponse)
async def ingest_to_kb(kb_id: str, file: UploadFile = File(...)):
    if not file.filename:
        raise HTTPException(400, "No filename")
    store = _get_kb_store()
    if store.get(kb_id) is None:
        raise HTTPException(404, f"kb {kb_id!r} not found")
    with tempfile.NamedTemporaryFile(delete=False, suffix=Path(file.filename).suffix) as tmp:
        content = await file.read()
        tmp.write(content)
        tmp_path = Path(tmp.name)
    try:
        # 用 KB-scoped pipeline（v0.3 多 KB 模式）
        pipeline = _RAGPipelineClass(kb_id=kb_id)
        result = pipeline.ingest_file(tmp_path, original_name=file.filename)
    finally:
        tmp_path.unlink(missing_ok=True)
    return IngestResponse(
        source_id=result["source_id"],
        chunks=result["chunks"],
        format=result["format"],
        filename=file.filename,
    )


@router.post("/sessions/{session_id}/kbs", response_model=SessionKbListResponse)
def bind_session_kbs(session_id: str, req: SessionKbBindRequest):
    store = _get_kb_store()
    try:
        store.bind_session(session_id, req.kb_ids)
    except ValueError as e:
        raise HTTPException(400, str(e))
    return SessionKbListResponse(
        session_id=session_id,
        kbs=[_kb_to_info(k) for k in store.get_session_kbs(session_id)],
    )


@router.get("/sessions/{session_id}/kbs", response_model=SessionKbListResponse)
def list_session_kbs(session_id: str):
    store = _get_kb_store()
    return SessionKbListResponse(
        session_id=session_id,
        kbs=[_kb_to_info(k) for k in store.get_session_kbs(session_id)],
    )
```

- [ ] **Step 5: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_api.py -v`
Expected: 全部 passed

- [ ] **Step 6: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/api/ tests/integration/test_api.py
git commit -m "feat(api): KB CRUD + ingest + session-KB binding endpoints"
```

### Task 11.7: CLI kb 子命令组

**Files:**
- Modify: `cli/main.py`
- Modify: `tests/integration/test_cli.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/integration/test_cli.py`:

```python
def test_kb_list_empty(runner):
    result = runner.invoke(app, ["kb", "list"])
    assert result.exit_code == 0
    assert "kbs" in result.stdout.lower() or "kb" in result.stdout.lower()


def test_kb_create_and_list(runner):
    result = runner.invoke(app, ["kb", "create", "CS101", "--description", "课程笔记"])
    assert result.exit_code == 0, result.output
    assert "cs101" in result.stdout.lower() or "CS101" in result.output

    result = runner.invoke(app, ["kb", "list"])
    assert result.exit_code == 0
    assert "cs101" in result.stdout.lower() or "CS101" in result.output


def test_kb_delete(runner):
    runner.invoke(app, ["kb", "create", "TMP"])
    result = runner.invoke(app, ["kb", "delete", "tmp"])
    assert result.exit_code == 0
    # 删完 list 应该不再有
    result = runner.invoke(app, ["kb", "list"])
    assert "tmp" not in result.stdout.lower()


def test_kb_ingest(runner, tmp_path):
    runner.invoke(app, ["kb", "create", "DOCS"])
    pdf = Path(__file__).resolve().parents[1] / "fixtures" / "sample.pdf"
    result = runner.invoke(app, ["kb", "ingest", "docs", str(pdf)])
    assert result.exit_code == 0, result.output
    assert "chunks" in result.output.lower() or "已入库" in result.output
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_cli.py -v`
Expected: 4 个新 test FAIL

- [ ] **Step 3: 改 cli/main.py 加 kb 子命令组**

修改 `cli/main.py`，在文件末尾（`if __name__` 之前）追加:

```python
from uni_rag.store.kb import KBStore as _KBStore
from uni_rag.config import load_settings as _load_settings
from uni_rag.rag.pipeline import RAGPipeline as _RAGPipeline


kb_app = typer.Typer(help="知识库管理（多 KB 隔离）")
app.add_typer(kb_app, name="kb")


@kb_app.command("list")
def kb_list():
    """列出所有知识库。"""
    store = _KBStore(_load_settings().kb_db_path)
    store.ensure_default()  # v0.2 → v0.3 自动迁移
    kbs = store.list()
    if not kbs:
        console.print("[dim]还没有 KB。试试: uni-rag kb create CS101[/dim]")
        return
    for k in kbs:
        desc = f" — {k['description']}" if k.get("description") else ""
        console.print(f"  [cyan]{k['id']}[/cyan]  {k['name']}{desc}")


@kb_app.command("create")
def kb_create(
    name: str = typer.Argument(...),
    description: str = typer.Option("", "--description", "-d", help="KB 描述"),
    id: str | None = typer.Option(None, "--id", help="自定义 KB ID（默认从 name 派生）"),
):
    """创建一个新知识库。"""
    store = _KBStore(_load_settings().kb_db_path)
    try:
        kb = store.create(name, description, kb_id=id)
    except ValueError as e:
        console.print(f"[red]✗ {e}[/red]")
        raise typer.Exit(1)
    console.print(Panel(
        f"[bold]{kb['name']}[/bold]\n"
        f"ID: {kb['id']}\n"
        f"描述: {kb['description'] or '(无)'}",
        title="✓ KB 已创建",
    ))


@kb_app.command("delete")
def kb_delete(kb_id: str = typer.Argument(...)):
    """删除一个知识库（连带删除该 KB 下的所有文档和索引）。"""
    store = _KBStore(_load_settings().kb_db_path)
    if not store.delete(kb_id):
        console.print(f"[red]✗ KB {kb_id!r} 不存在[/red]")
        raise typer.Exit(1)
    # 清理磁盘（uploads/、chroma/、bm25/）
    import shutil
    kb_base = _load_settings().data_dir / "kbs" / kb_id
    if kb_base.exists():
        shutil.rmtree(kb_base)
    console.print(f"[green]✓ 已删除 KB {kb_id}[/green]")


@kb_app.command("ingest")
def kb_ingest(
    kb_id: str = typer.Argument(...),
    file: Path = typer.Argument(..., exists=True),
):
    """上传并入库一个文档到指定 KB。"""
    store = _KBStore(_load_settings().kb_db_path)
    if store.get(kb_id) is None:
        console.print(f"[red]✗ KB {kb_id!r} 不存在。先 uni-rag kb create {kb_id}[/red]")
        raise typer.Exit(1)
    pipeline = _RAGPipeline(kb_id=kb_id)
    result = pipeline.ingest_file(file, original_name=file.name)
    console.print(Panel(
        f"[bold]{file.name}[/bold] → KB [cyan]{kb_id}[/cyan]\n"
        f"格式: {result['format']}\n"
        f"chunks: {result['chunks']}\n"
        f"ID: {result['source_id']}",
        title="✓ 已入库",
    ))
```

- [ ] **Step 4: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_cli.py -v`
Expected: 全部 passed

- [ ] **Step 5: 手动 smoke test**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
uv run uni-rag kb list
uv run uni-rag kb create "CS101" --description "算法课"
uv run uni-rag kb create "KAOYAN" --description "考研真题"
uv run uni-rag kb list
uv run uni-rag kb ingest cs101 tests/fixtures/sample.pdf
uv run uni-rag kb delete cs101
```

- [ ] **Step 6: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add cli/main.py tests/integration/test_cli.py
git commit -m "feat(cli): kb subcommand group (list/create/delete/ingest)"
```

### Task 11.8: 前端 KB 切换 UI（最小可用）

**Files:**
- Modify: `src/uni_rag/web/index.html`
- Modify: `src/uni_rag/web/app.js`
- Modify: `src/uni_rag/web/style.css`

- [ ] **Step 1: 在 index.html 的 upload-section 前加 KB 选择器**

在 `<section id="upload-section">` 之前插入:

```html
<section id="kb-section">
  <h2>0. 选择知识库</h2>
  <div class="kb-bar">
    <select id="kb-select">
      <option value="">（加载中…）</option>
    </select>
    <input type="text" id="kb-new-name" placeholder="新 KB 名称" />
    <button type="button" id="kb-create-btn">新建</button>
  </div>
</section>
```

- [ ] **Step 2: 改 app.js 加载 KB 列表 + 切换**

在文件顶部 `let sessionId = null;` 之后追加:

```javascript
let currentKbId = '';

async function loadKbs() {
  const sel = document.getElementById('kb-select');
  sel.innerHTML = '';
  try {
    const r = await fetch('/api/kbs');
    const data = await r.json();
    if (!data.kbs.length) {
      sel.innerHTML = '<option value="">（还没有 KB）</option>';
      return;
    }
    data.kbs.forEach((kb) => {
      const opt = document.createElement('option');
      opt.value = kb.id;
      opt.textContent = `${kb.id} — ${kb.name}`;
      sel.appendChild(opt);
    });
    // 默认选 default
    const def = data.kbs.find((k) => k.id === 'default') || data.kbs[0];
    sel.value = def.id;
    currentKbId = def.id;
  } catch (e) {
    sel.innerHTML = '<option value="">（加载失败）</option>';
  }
}

document.getElementById('kb-select').addEventListener('change', (e) => {
  currentKbId = e.target.value;
  // 切 KB → 清空聊天（新 KB 不继承旧 session 上下文）
  sessionId = null;
  document.getElementById('chat').innerHTML = '';
  document.getElementById('file-list').innerHTML = '';
});

document.getElementById('kb-create-btn').addEventListener('click', async () => {
  const name = document.getElementById('kb-new-name').value.trim();
  if (!name) return;
  const r = await fetch('/api/kbs', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ name }),
  });
  if (!r.ok) {
    alert('创建失败');
    return;
  }
  document.getElementById('kb-new-name').value = '';
  await loadKbs();
});

loadKbs();
```

- [ ] **Step 3: 改 uploadFile / form submit 走 KB 路由**

替换 `uploadFile` 里的 endpoint:

```javascript
async function uploadFile(file) {
  const formData = new FormData();
  formData.append('file', file);
  const url = currentKbId
    ? `/api/kbs/${encodeURIComponent(currentKbId)}/ingest`
    : '/api/ingest';
  const r = await fetch(url, { method: 'POST', body: formData });
  if (!r.ok) {
    alert('上传失败');
    return;
  }
  const data = await r.json();
  sources.set(file.name, data.source_id);
  const li = document.createElement('li');
  li.textContent = `${file.name} · ${data.chunks} 块 · KB ${currentKbId || 'default'}`;
  fileList.appendChild(li);
}
```

替换 form submit 里的 fetch（保留 query 走 v0.2 全局 — 因为 query 端点尚未 KB-scoped；这是 v0.3.1 的工作）:

```javascript
  // v0.3: 仍用全局 query；多 KB session 路由留到后续
  const r = await fetch('/api/query', { ... });
```

> **注：** v0.3 不重写 `/api/query` 路由；多 KB 隔离在 CLI + ingestion 端到端，query 端点继续走默认 KB。这避免把 query 路径也拆开，scope 控制清晰。如果要 query-scoped，可在 v0.3 收尾后开 v0.3.1 加 `POST /api/kbs/{id}/query`。

- [ ] **Step 4: 加 style.css**

在 `style.css` 末尾追加:

```css
.kb-bar { display: flex; gap: 0.5rem; align-items: center; }
#kb-select, #kb-new-name {
  padding: 0.5rem 0.7rem;
  border: 1px solid var(--border);
  background: white;
  font-family: inherit;
  font-size: 0.95rem;
}
#kb-select { flex: 1; }
#kb-new-name { flex: 1; }
#kb-create-btn {
  padding: 0.5rem 1rem;
  background: var(--accent);
  color: white;
  border: none;
  cursor: pointer;
  font-family: inherit;
}
#kb-create-btn:hover { background: #4a3829; }
```

- [ ] **Step 5: 手动验证**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
uv run uni-rag serve
```

打开 `http://127.0.0.1:8765/`，看到 "0. 选择知识库" 段、KB 下拉、创建按钮。点新建 → 列表刷新。切 KB → 聊天和文件列表清空。

- [ ] **Step 6: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/web/
git commit -m "feat(web): KB selector + create UI"
```

### Task 11.9: v0.2 → v0.3 默认 KB 自动迁移

**说明：** v0.3 启动时第一次访问 KB API 或 CLI `kb list` 时，`KBStore.ensure_default()` 会自动创建 `default` KB 记录。v0.2 的旧 Chroma collection `chunks` + BM25 `data/bm25/docs.pkl` 不动，访问 default KB 时直接走 v0.2 路径（kb_id=None）。

这意味着 v0.2 用户升级 v0.3 不需要数据迁移 — 旧数据自动成为 default KB 的内容。CLI `kb list` 第一次会自动创建 default 行。

Task 11.6 步骤 4 里的 `kb_list` CLI 已调用 `ensure_default()`；Task 11.7 的 `loadKbs` 前端调 `/api/kbs` 也会触发（v0.3.0 暂不在 routes 启动时 ensure，依赖客户端调用时创建）。

- [ ] **Step 1: 在 api/app.py 启动时 ensure default KB**

修改 `src/uni_rag/api/app.py` 的 `create_app` 函数，在 `app.include_router(router)` 之前追加:

```python
    # v0.3 migration: ensure default KB exists on startup
    from uni_rag.store.kb import KBStore
    from uni_rag.config import load_settings
    try:
        KBStore(load_settings().kb_db_path).ensure_default()
    except Exception:
        # 不阻塞服务启动
        pass
```

- [ ] **Step 2: 跑全部测试无回归**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest -v`
Expected: 全部 passed (v0.1: 43 + v0.2: 8 + v0.3: ~25 ≈ 76 tests)

- [ ] **Step 3: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/api/app.py
git commit -m "feat(api): auto-create default KB on app startup (v0.2 migration)"
```

---

## Milestone 12: v0.3 验收

### Task 12.1: BDD 端到端 + Coverage

**Files:**
- Create: `tests/bdd/test_b8_b10.py`

- [ ] **Step 1: 写 BDD 端到端**

```python
# tests/bdd/test_b8_b10.py
"""End-to-end BDD verification for B8-B10."""
import pytest
from pathlib import Path
from fastapi.testclient import TestClient
from uni_rag.api.app import create_app


@pytest.fixture
def client(tmp_path, monkeypatch):
    monkeypatch.setenv("UNI_RAG_DATA_DIR", str(tmp_path))
    monkeypatch.setenv("ANTHROPIC_API_KEY", "test-key")
    app = create_app()
    return TestClient(app)


def test_b8_export_answer_markdown(client, tmp_path, monkeypatch):
    """B8: 问问题 → 下载 .md 文件，含问题、答案、引用。"""
    monkeypatch.setattr(
        "uni_rag.llm.client.LLMClient.complete",
        lambda *a, **kw: "answer",
    )
    pdf = Path(__file__).resolve().parents[1] / "fixtures" / "sample.pdf"
    with open(pdf, "rb") as f:
        client.post("/api/ingest", files={"file": ("sample.pdf", f, "application/pdf")})
    client.post("/api/query", json={"question": "q1"})

    import sqlite3
    from uni_rag.config import load_settings
    with sqlite3.connect(load_settings().sessions_db_path) as conn:
        sid = conn.execute("SELECT DISTINCT session_id FROM messages LIMIT 1").fetchone()[0]

    r = client.get(f"/api/sessions/{sid}/messages/1/export?format=md")
    assert r.status_code == 200
    assert "text/markdown" in r.headers["content-type"]
    assert "问答记录" in r.text


def test_b8_export_answer_pdf(client, tmp_path, monkeypatch):
    """B8: PDF 导出返回有效 PDF bytes。"""
    monkeypatch.setattr(
        "uni_rag.llm.client.LLMClient.complete",
        lambda *a, **kw: "answer",
    )
    pdf = Path(__file__).resolve().parents[1] / "fixtures" / "sample.pdf"
    with open(pdf, "rb") as f:
        client.post("/api/ingest", files={"file": ("sample.pdf", f, "application/pdf")})
    client.post("/api/query", json={"question": "q1"})

    import sqlite3
    from uni_rag.config import load_settings
    with sqlite3.connect(load_settings().sessions_db_path) as conn:
        sid = conn.execute("SELECT DISTINCT session_id FROM messages LIMIT 1").fetchone()[0]

    r = client.get(f"/api/sessions/{sid}/messages/1/export?format=pdf")
    assert r.status_code == 200
    assert r.headers["content-type"] == "application/pdf"
    assert r.content.startswith(b"%PDF-")


def test_b10_kb_isolation_end_to_end(client, tmp_path, monkeypatch):
    """B10: 两个 KB 的内容互不污染。"""
    monkeypatch.setattr(
        "uni_rag.llm.client.LLMClient.complete",
        lambda *a, **kw: "ok",
    )
    # 创建两个 KB
    client.post("/api/kbs", json={"name": "ALPHA"})
    client.post("/api/kbs", json={"name": "BETA"})

    pdf = Path(__file__).resolve().parents[1] / "fixtures" / "sample.pdf"
    with open(pdf, "rb") as f:
        r = client.post(
            "/api/kbs/alpha/ingest",
            files={"file": ("alpha.pdf", f, "application/pdf")},
        )
        assert r.status_code == 200, r.text

    with open(pdf, "rb") as f:
        r = client.post(
            "/api/kbs/beta/ingest",
            files={"file": ("beta.pdf", f, "application/pdf")},
        )
        assert r.status_code == 200, r.text

    # 验证两个 KB 的 chroma collection 独立
    from uni_rag.config import load_settings
    data_dir = load_settings().data_dir
    assert (data_dir / "kbs" / "alpha" / "chroma").exists()
    assert (data_dir / "kbs" / "beta" / "chroma").exists()
    assert (data_dir / "kbs" / "alpha" / "uploads" / "alpha.pdf").exists()
    assert (data_dir / "kbs" / "beta" / "uploads" / "beta.pdf").exists()
    # alpha 的 uploads 里不应该有 beta.pdf
    assert not (data_dir / "kbs" / "alpha" / "uploads" / "beta.pdf").exists()
```

- [ ] **Step 2: 跑 BDD**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/bdd/test_b8_b10.py -v`
Expected: 3 passed

- [ ] **Step 3: 跑全套 + coverage**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest --cov=src/uni_rag --cov-report=term-missing`
Expected: coverage ≥ 85%（v0.3 新模块覆盖完整）

- [ ] **Step 4: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add tests/bdd/test_b8_b10.py
git commit -m "test: BDD end-to-end B8 (export) + B10 (multi-KB)"
```

### Task 12.2: 手动端到端验证（PM 验收）

**M9 验证（导出答案）：**
1. `uv run uni-rag serve`
2. 浏览器开 `http://127.0.0.1:8765/`
3. 上传 sample.pdf → 问一个问题
4. 在 assistant 消息底部看到 "Download .md" 和 "Download .pdf" 链接
5. 点 .md → 下载文件，用文本编辑器打开，看到 "问答记录 / 问题 / 答案 / 引用 / chunk 文本"
6. 点 .pdf → 下载文件，用 Preview 打开，看到带样式的中文排版
7. CLI: `uv run uni-rag ask "test"` 后从 sessions.db 找 session_id，直接 curl `http://127.0.0.1:8766/api/sessions/<sid>/messages/1/export?format=md` 也可

**M10 验证（Docker）：**
1. `cp .env.example .env` → 填 `ANTHROPIC_API_KEY`
2. `docker compose up -d`（首次构建 5-10 分钟下载模型）
3. `docker compose logs -f` 看启动 → 直到看到 "Application startup complete"
4. 浏览器开 `http://127.0.0.1:8766/` → 能上传 + 问答
5. `docker compose down` → 再 `docker compose up -d` → 数据还在（volume 持久化）

**M11 验证（多 KB）：**
1. CLI:
   ```
   uv run uni-rag kb list
   uv run uni-rag kb create "CS101" --description "算法课笔记"
   uv run uni-rag kb create "KAOYAN" --description "考研真题"
   uv run uni-rag kb list  # 看到 3 个：default, cs101, kaoyan
   uv run uni-rag kb ingest cs101 tests/fixtures/sample.pdf
   uv run uni-rag kb ingest kaoyan tests/fixtures/sample.pdf
   ```
2. 看 `data/kbs/cs101/uploads/sample.pdf` 和 `data/kbs/kaoyan/uploads/sample.pdf` 都存在
3. 浏览器：KB 下拉选 cs101 → 上传一个 PDF → 切到 kaoyan → 看不到 cs101 的文件
4. 删除 KB: `uv run uni-rag kb delete cs101` → `data/kbs/cs101/` 整个目录被删

---

## 关键参考

- **v0.1 计划**: `docs/superpowers/plans/2026-06-17-uni-rag-v0.1.md`
- **v0.2 计划**: `docs/superpowers/plans/2026-06-17-uni-rag-v0.2.md`
- **weasyprint**: https://weasyprint.org/ — 纯 Python，HTML/CSS → PDF
- **Docker uv image**: `ghcr.io/astral-sh/uv:latest`
- **Chroma collection**: 每 KB 一个 collection name，name 规则 `[a-z0-9_]{3,63}`（满足 `kb_<id>` 6-35 字符）
- **BGE-M3 / bge-reranker-base**: 已在 v0.1/v0.2 验证，Docker 构建期 `SentenceTransformer(...)` 一次预热

## 风险与回退

1. **weasyprint 装包失败**: `libpango` 在某些 Linux 发行版需手动装；Dockerfile 已显式 apt install。回退：保留 MD 导出，PDF 标记 v0.3.1
2. **Docker 镜像太大**: 基础镜像 ~150MB + 依赖 ~800MB + 模型 ~2GB = ~3GB。预下载模型是值得的（首次请求不再冷启动）
3. **KB ID slug 冲突**: `CS101` 和 `cs101` 都会被 slugify 成 `cs101`。解决：CLI `create` 时若冲突报 `exists` 错误（已实现）；用户传 `--id` 可显式指定
4. **多 KB query 路由**: v0.3 query 端点未 KB-scoped（默认 KB）。这是有意 trade-off：避免改 query 路径导致 v0.2 客户端挂掉。v0.3.1 可加 `POST /api/kbs/{id}/query`
5. **session_kbs 级联删除**: 删除 KB 时已通过外键 `ON DELETE CASCADE` 自动清理 session_kbs 行；但 session message 本身仍保留（KB 内容没了，引用会失效）— 这是 v0.3 的已知边界
