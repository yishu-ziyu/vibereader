# uni-rag v0.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.1 已跑通 (43 tests, 96% coverage)。v0.2 在 v0.1 之上加 3 个独立特性：(1) 点击引用打开侧栏看高亮原文 (Notion AI 风格)、(2) DOCX 表格 + 公式解析、(3) Session 长度管理防止 LLM context 爆。

**Architecture:** 完全沿用 v0.1，不动核心 RAG 流程。Feature 1 加一条 GET /api/documents/{filename}/chunks 端点和前端 side-panel；Feature 2 改 _parse_docx；Feature 3 在 SessionStore 加 get_recent + Settings 加 max_session_messages。

**Tech Stack:** 同 v0.1。Feature 2 用 python-docx 的 `doc.tables` 拿表格。Feature 1 前端用 vanilla JS + CSS slide-in。

**BDD Coverage:**
- **B5 点击引用看原文** → M6
- **B6 DOCX 表格+公式解析** → M7
- **B7 长 session 不爆 context** → M8

---

## File Structure

v0.2 涉及的新增/修改文件（其余沿用 v0.1）：

```
uni-rag/
├── src/uni_rag/
│   ├── config.py                  # M8: +max_session_messages
│   ├── ingest/parsers.py          # M7: 改 _parse_docx
│   ├── session/store.py           # M8: +get_recent
│   ├── rag/pipeline.py            # M8: 用 get_recent
│   ├── api/
│   │   ├── routes.py              # M6: +/api/documents/{filename}/chunks
│   │   └── schemas.py             # M6: +ChunkInfo, DocumentChunksResponse
│   └── web/
│       ├── index.html             # M6: +<aside id="doc-panel">
│       ├── app.js                 # M6: 改 citations 为可点击 + side panel
│       └── style.css              # M6: +slide-in aside
└── tests/
    ├── fixtures/
    │   └── sample_with_table_formula.docx   # M7 新增
    ├── unit/
    │   ├── test_parsers.py        # M7: +table, +formula
    │   └── test_session.py        # M8: +get_recent
    └── integration/
        ├── test_api.py            # M6: +/documents/{f}/chunks
        └── test_query_pipeline.py # M8: +long session
    └── scripts/
        └── make_table_formula_fixture.py    # M7 新增
```

---

## Milestone 6: 高亮引用 (B5)

**目的:** 用户点引用 chip → 右侧侧栏滑出 → 显示原文 + 高亮命中 span。Notion AI 风格。

### Task 6.1: 文档 chunks API 端点

**Files:**
- Modify: `src/uni_rag/api/schemas.py`
- Modify: `src/uni_rag/api/routes.py`
- Modify: `tests/integration/test_api.py`

- [ ] **Step 1: 写失败的 API 测试**

追加到 `tests/integration/test_api.py`:

```python
def test_get_document_chunks(client, tmp_path, monkeypatch):
    """GET /api/documents/{filename}/chunks returns the file's chunks."""
    def fake_complete(self, system, max_tokens=1024):
        return "ok"
    monkeypatch.setattr("uni_rag.llm.client.LLMClient.complete", fake_complete)

    pdf = Path(__file__).resolve().parents[1] / "fixtures" / "sample.pdf"
    with open(pdf, "rb") as f:
        r = client.post("/api/ingest", files={"file": ("sample.pdf", f, "application/pdf")})
    assert r.status_code == 200

    r = client.get("/api/documents/sample.pdf/chunks")
    assert r.status_code == 200
    data = r.json()
    assert "chunks" in data
    assert len(data["chunks"]) > 0
    first = data["chunks"][0]
    assert {"id", "text", "span"} <= set(first.keys())
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_api.py::test_get_document_chunks -v`
Expected: FAIL with 404

- [ ] **Step 3: 加 schemas**

在 `src/uni_rag/api/schemas.py` 末尾追加:

```python
class ChunkInfo(BaseModel):
    """A single chunk of a document, for the side-panel viewer."""
    id: str
    text: str
    span: tuple[int, int] | None = None
    section: str = ""


class DocumentChunksResponse(BaseModel):
    """All chunks of a single document, ordered by offset."""
    filename: str
    chunks: list[ChunkInfo]
```

- [ ] **Step 4: 加 route**

修改 `src/uni_rag/api/routes.py`，先把 `VectorStore` import 加进去，并在 router 里追加:

```python
from uni_rag.store.vector import VectorStore


@router.get("/documents/{filename}/chunks", response_model=DocumentChunksResponse)
def get_document_chunks(filename: str):
    """Return all chunks for a given uploaded filename, ordered by offset."""
    settings = load_settings()
    # 验证文件存在
    target = settings.uploads_dir / filename
    if not target.exists():
        raise HTTPException(404, f"Document not found: {filename}")

    # 从 Chroma 拉这个 source 的所有 chunk
    vector = VectorStore()
    res = vector.collection.get(where={"source": filename}, include=["metadatas", "documents"])
    if not res["ids"]:
        return DocumentChunksResponse(filename=filename, chunks=[])

    rows = []
    for i, cid in enumerate(res["ids"]):
        meta = res["metadatas"][i] if res["metadatas"] else {}
        doc_text = res["documents"][i] if res["documents"] else ""
        start = meta.get("start", 0)
        end = meta.get("end", start + len(doc_text))
        rows.append({
            "id": cid,
            "text": doc_text,
            "span": [start, end],
            "section": meta.get("section", ""),
        })
    rows.sort(key=lambda r: r["span"][0] if r["span"] else 0)
    return DocumentChunksResponse(filename=filename, chunks=rows)
```

> **注意**：`VectorStore.collection` 已在 v0.1 暴露（`test_vector.py` 用了 `collection.get()`），安全。

- [ ] **Step 5: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_api.py -v`
Expected: 3 passed (含新增的)

- [ ] **Step 6: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/api/ tests/integration/test_api.py
git commit -m "feat(api): GET /api/documents/{filename}/chunks for side panel"
```

### Task 6.2: 前端 side panel — HTML + CSS

**Files:**
- Modify: `src/uni_rag/web/index.html`
- Modify: `src/uni_rag/web/style.css`

- [ ] **Step 1: 在 index.html 加 aside 元素**

在 `index.html` 末尾 `</body>` 之前插入:

```html
<aside id="doc-panel" class="hidden" aria-hidden="true">
  <div class="panel-header">
    <h3 id="panel-title">原文</h3>
    <button id="panel-close" type="button" aria-label="关闭">×</button>
  </div>
  <div id="panel-body" class="panel-body"></div>
</aside>
```

- [ ] **Step 2: 在 style.css 加侧栏样式**

在 `style.css` 末尾追加:

```css
/* Side panel for citation viewing */
#doc-panel {
  position: fixed;
  top: 0;
  right: 0;
  width: 420px;
  max-width: 90vw;
  height: 100vh;
  background: var(--paper);
  border-left: 1px solid var(--border);
  box-shadow: -4px 0 16px rgba(0, 0, 0, 0.08);
  transform: translateX(100%);
  transition: transform 0.25s ease-out;
  z-index: 100;
  display: flex;
  flex-direction: column;
}
#doc-panel.open { transform: translateX(0); }
#doc-panel.hidden { display: none; }

.panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem 1.2rem;
  border-bottom: 1px solid var(--border);
  background: var(--soft);
}
.panel-header h3 { font-size: 1rem; color: var(--accent); }
#panel-close {
  background: none;
  border: none;
  font-size: 1.5rem;
  cursor: pointer;
  color: var(--ink);
  line-height: 1;
}

.panel-body {
  flex: 1;
  overflow-y: auto;
  padding: 1.2rem;
  line-height: 1.7;
  font-size: 0.95rem;
  white-space: pre-wrap;
}

.panel-body mark {
  background: #f7e4a3;
  color: var(--ink);
  padding: 2px 4px;
  border-radius: 2px;
  box-decoration-break: clone;
  -webkit-box-decoration-break: clone;
}

.panel-body .section-title {
  display: block;
  font-weight: 600;
  color: var(--accent);
  margin-top: 1rem;
  margin-bottom: 0.3rem;
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/web/index.html src/uni_rag/web/style.css
git commit -m "feat(web): side panel HTML + CSS for citation viewer"
```

### Task 6.3: 前端 — 引用 chip 可点击 + 拉原文 + 高亮

**Files:**
- Modify: `src/uni_rag/web/app.js`

- [ ] **Step 1: 重写 addMessage 让引用可点击**

完全替换 `src/uni_rag/web/app.js` 的 `addMessage` 函数 + 末尾追加 side-panel 控制逻辑:

```javascript
// 替换 addMessage 整个函数
function addMessage(role, text, citations = []) {
  const div = document.createElement('div');
  div.className = `message ${role}`;
  const content = document.createElement('div');
  content.className = 'content';
  content.textContent = text;
  div.appendChild(content);

  if (citations.length) {
    const cits = document.createElement('div');
    cits.className = 'citations';
    citations.forEach((c, i) => {
      const chip = document.createElement('button');
      chip.type = 'button';
      chip.className = 'cite-chip';
      chip.textContent = `[${i + 1}] ${c.source}${c.section ? ' · ' + c.section : ''}`;
      chip.addEventListener('click', () => openCitation(c.source, c.span, c.text));
      cits.appendChild(chip);
    });
    div.appendChild(cits);
  }
  chat.appendChild(div);
  chat.scrollTop = chat.scrollHeight;
}

// Side panel 控制
const docPanel = document.getElementById('doc-panel');
const panelTitle = document.getElementById('panel-title');
const panelBody = document.getElementById('panel-body');
const panelClose = document.getElementById('panel-close');

panelClose.addEventListener('click', closePanel);
document.addEventListener('keydown', (e) => {
  if (e.key === 'Escape') closePanel();
});

function openCitation(filename, span, citedText) {
  panelTitle.textContent = filename;
  panelBody.innerHTML = '<p style="color:var(--accent)">加载中…</p>';
  docPanel.classList.remove('hidden');
  docPanel.classList.add('open');
  docPanel.setAttribute('aria-hidden', 'false');

  fetch(`/api/documents/${encodeURIComponent(filename)}/chunks`)
    .then((r) => {
      if (!r.ok) throw new Error('加载失败');
      return r.json();
    })
    .then((data) => renderDocument(data.chunks, span, citedText))
    .catch((err) => {
      panelBody.innerHTML = `<p style="color:#a44">${err.message}</p>`;
    });
}

function renderDocument(chunks, highlightSpan, highlightText) {
  panelBody.innerHTML = '';
  if (!chunks.length) {
    panelBody.innerHTML = '<p>无内容</p>';
    return;
  }
  // 找到最贴近 highlightSpan 的 chunk 来高亮
  const target = chunks.find((c) =>
    c.span && highlightSpan &&
    c.span[0] <= highlightSpan[0] && c.span[1] >= highlightSpan[1]
  ) || chunks[0];

  chunks.forEach((c) => {
    if (c.section) {
      const t = document.createElement('span');
      t.className = 'section-title';
      t.textContent = c.section;
      panelBody.appendChild(t);
    }
    const p = document.createElement('p');
    if (c.id === target.id && c.text) {
      // 把高亮 span 用 <mark> 包裹
      p.innerHTML = wrapHighlight(c.text, highlightSpan, c.span);
    } else {
      p.textContent = c.text;
    }
    panelBody.appendChild(p);
  });

  // 滚到高亮位置
  const markEl = panelBody.querySelector('mark');
  if (markEl) markEl.scrollIntoView({ block: 'center', behavior: 'smooth' });
}

function wrapHighlight(text, absSpan, chunkSpan) {
  // 把 text 在 chunkSpan 内的 offset 投影到 absolute offset
  // 简化：直接在 text 里找 citedText 第一个出现位置包 <mark>
  if (!absSpan) return escapeHtml(text);
  const esc = escapeHtml(text);
  // 计算相对偏移
  const relStart = Math.max(0, absSpan[0] - chunkSpan[0]);
  const relEnd = Math.min(text.length, absSpan[1] - chunkSpan[0]);
  if (relStart >= relEnd) return esc;
  return (
    escapeHtml(text.slice(0, relStart)) +
    '<mark>' + escapeHtml(text.slice(relStart, relEnd)) + '</mark>' +
    escapeHtml(text.slice(relEnd))
  );
}

function escapeHtml(s) {
  return s.replace(/[&<>"']/g, (ch) => ({
    '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;',
  })[ch]);
}

function closePanel() {
  docPanel.classList.remove('open');
  docPanel.classList.add('hidden');
  docPanel.setAttribute('aria-hidden', 'true');
  panelBody.innerHTML = '';
}
```

- [ ] **Step 2: 在 style.css 加 cite-chip 样式**

在 `style.css` 末尾追加:

```css
.cite-chip {
  display: inline-block;
  margin: 0 0.3rem 0.3rem 0;
  padding: 0.2rem 0.6rem;
  background: var(--soft);
  border: 1px solid var(--border);
  border-radius: 3px;
  font-size: 0.82rem;
  font-family: inherit;
  color: var(--accent);
  cursor: pointer;
  transition: background 0.15s;
}
.cite-chip:hover { background: #e0d8c8; }
```

- [ ] **Step 3: 手动验证**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
uv run uni-rag serve
```

打开 `http://127.0.0.1:8765/`，上传 sample.pdf，问一个问题，看到 assistant 消息底部出现引用 chip，点击 chip → 右侧侧栏滑出 → 高亮 chunk 文字。

- [ ] **Step 4: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/web/
git commit -m "feat(web): clickable citation chips + side panel with highlight"
```

---

## Milestone 7: DOCX 表格 + 公式 (B6)

**目的:** DOCX 文件能正确解析表格（保留 markdown `|` 结构）和文本公式（`$...$`、`$$...$$` 形式）。

### Task 7.1: 生成 fixture (含 2x2 表格 + 公式段落)

**Files:**
- Create: `scripts/make_table_formula_fixture.py`
- Create: `tests/fixtures/sample_with_table_formula.docx` (脚本产出)

- [ ] **Step 1: 写生成器脚本**

```python
# scripts/make_table_formula_fixture.py
"""Generate a DOCX fixture with a 2x2 table and a math-formula paragraph."""
import docx
from pathlib import Path

OUT = Path(__file__).resolve().parent.parent / "tests" / "fixtures" / "sample_with_table_formula.docx"


def main():
    d = docx.Document()

    d.add_heading("Chapter 1: Formulas and Tables", level=1)
    d.add_paragraph("This chapter introduces the quadratic equation and a comparison table.")

    d.add_heading("Section 1.1: Quadratic Formula", level=2)
    d.add_paragraph("The well-known identity is: $x^2 + y^2 = z^2$ (Pythagoras).")
    d.add_paragraph("For integrals we write: $$\\int_0^1 f(x) dx$$ in display form.")

    d.add_heading("Section 1.2: Comparison", level=2)
    d.add_paragraph("The table below compares supervised and unsupervised learning:")

    # 2x2 table
    table = d.add_table(rows=2, cols=2)
    table.cell(0, 0).text = "Aspect"
    table.cell(0, 1).text = "Description"
    table.cell(1, 0).text = "Supervised"
    table.cell(1, 1).text = "Uses labeled data"

    d.add_paragraph("End of document.")

    OUT.parent.mkdir(parents=True, exist_ok=True)
    d.save(str(OUT))
    print(f"Wrote {OUT}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 跑脚本生成 fixture**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run python scripts/make_table_formula_fixture.py`
Expected: `Wrote tests/fixtures/sample_with_table_formula.docx`

- [ ] **Step 3: Commit (脚本)**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add scripts/make_table_formula_fixture.py tests/fixtures/sample_with_table_formula.docx
git commit -m "test(fix): DOCX fixture with table + formula"
```

### Task 7.2: 改 _parse_docx 解析表格 + 公式

**Files:**
- Modify: `src/uni_rag/ingest/parsers.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/unit/test_parsers.py`:

```python
FIXTURES = Path(__file__).resolve().parents[1] / "fixtures"


def test_parse_docx_with_table():
    """DOCX 解析必须把 2x2 表格变成 markdown | 分隔的多行。"""
    doc = parse_document(FIXTURES / "sample_with_table_formula.docx")
    assert "Aspect" in doc.text
    assert "Description" in doc.text
    # markdown 风格：包含 | 分隔
    assert " | " in doc.text
    # 至少两行表格
    table_lines = [l for l in doc.text.split("\n") if " | " in l]
    assert len(table_lines) >= 2


def test_parse_docx_with_formula():
    """DOCX 解析必须保留 $...$ 公式（原样保留或包到 code block 都算通过）。"""
    doc = parse_document(FIXTURES / "sample_with_table_formula.docx")
    # 内联公式原文保留
    assert "x^2 + y^2 = z^2" in doc.text
    # 块公式：用 ``` 围起来
    assert "```" in doc.text
    # 且包含积分符号或 LaTeX 关键字
    assert "int_0^1" in doc.text or "int" in doc.text
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_parsers.py -v`
Expected: 2 个新 test 都 FAIL

- [ ] **Step 3: 改 _parse_docx**

在 `src/uni_rag/ingest/parsers.py` 顶部 `import re` 那块加 `import re`（如果还没有），然后完全替换 `_parse_docx`:

```python
import re  # 顶部已存在则忽略


_FORMULA_INLINE_RE = re.compile(r"\$([^$\n]+)\$")
_FORMULA_BLOCK_RE = re.compile(r"\$\$([^$]+)\$\$", re.DOTALL)


def _parse_docx(path: Path) -> ParsedDocument:
    d = docx.Document(str(path))

    parts: list[str] = []

    # 1. 段落（按 body 顺序）
    for p in d.paragraphs:
        text = p.text.strip()
        if not text:
            continue
        text = _wrap_formulas(text)
        parts.append(text)

    # 2. 表格（按 body 顺序；用 d.element.body 顺序，简化：放在段落后）
    for table in d.tables:
        rows = []
        for row in table.rows:
            cells = [cell.text.strip() for cell in row.cells]
            rows.append(" | ".join(cells))
        if rows:
            parts.append("\n".join(rows))

    return ParsedDocument(
        text="\n\n".join(parts),
        format="docx",
        source_path=str(path),
    )


def _wrap_formulas(text: str) -> str:
    """把 $$...$$ 块公式包到 ``` 围栏里；$...$ 行内公式保留。"""
    def block_sub(m: re.Match) -> str:
        return f"```\n{m.group(1).strip()}\n```"
    return _FORMULA_BLOCK_RE.sub(block_sub, text)
```

- [ ] **Step 4: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_parsers.py -v`
Expected: 5 passed (含原 3 个 + 新 2 个)

- [ ] **Step 5: 跑全套确认没回归**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest -v`
Expected: 全部 passed

- [ ] **Step 6: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/ingest/parsers.py tests/unit/test_parsers.py
git commit -m "feat(parsers): DOCX extracts tables (md-style) and wraps $$...$$ formulas"
```

---

## Milestone 8: Session 长度管理 (B7)

**目的:** 长对话不爆 LLM context。SessionStore 加 get_recent，pipeline 在 query() 时只拿最近 N 条。

### Task 8.1: Settings 加 max_session_messages

**Files:**
- Modify: `src/uni_rag/config.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/unit/test_config.py` 追加:

```python
def test_max_session_messages_default():
    from uni_rag.config import load_settings
    s = load_settings()
    assert s.max_session_messages == 20


def test_max_session_messages_override(monkeypatch):
    monkeypatch.setenv("UNI_RAG_MAX_SESSION_MESSAGES", "5")
    # 清掉单例以触发重新加载
    import uni_rag.config as cfg
    cfg._settings = None
    s = load_settings()
    assert s.max_session_messages == 5
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_config.py -v`
Expected: 2 new tests FAIL with `AttributeError`

- [ ] **Step 3: 在 Settings 加字段**

修改 `src/uni_rag/config.py`，在 `Settings` 类里追加:

```python
    # Session 长度上限
    max_session_messages: int = 20
```

- [ ] **Step 4: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_config.py -v`
Expected: 5 passed (含 3 个原 + 2 个新)

- [ ] **Step 5: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/config.py tests/unit/test_config.py
git commit -m "feat(config): max_session_messages setting (default 20)"
```

### Task 8.2: SessionStore.get_recent

**Files:**
- Modify: `src/uni_rag/session/store.py`
- Modify: `tests/unit/test_session.py`

- [ ] **Step 1: 写失败测试**

追加到 `tests/unit/test_session.py`:

```python
def test_get_recent_returns_last_n(store):
    sid = store.create()
    for i in range(5):
        store.append(sid, "user", f"q{i}")
        store.append(sid, "assistant", f"a{i}")
    # 总 10 条，取最后 4 条
    recent = store.get_recent(sid, limit=4)
    assert len(recent) == 4
    assert recent[0]["content"] == "q3"
    assert recent[-1]["content"] == "a4"


def test_get_recent_handles_smaller_history(store):
    sid = store.create()
    store.append(sid, "user", "only")
    # 不足 limit 时返回全部
    recent = store.get_recent(sid, limit=10)
    assert len(recent) == 1
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_session.py -v`
Expected: 2 new tests FAIL with `AttributeError`

- [ ] **Step 3: 加 get_recent 方法**

修改 `src/uni_rag/session/store.py`，在 `clear` 方法后追加:

```python
    def get_recent(self, session_id: str, limit: int) -> list[dict]:
        """Return the last `limit` messages for a session, ordered by seq ASC.

        If history has fewer than `limit` messages, returns all of them.
        """
        if limit <= 0:
            return []
        with sqlite3.connect(self.db_path) as conn:
            rows = conn.execute(
                """
                SELECT role, content FROM (
                    SELECT role, content, seq FROM messages
                    WHERE session_id = ?
                    ORDER BY seq DESC
                    LIMIT ?
                ) ORDER BY seq ASC
                """,
                (session_id, limit),
            ).fetchall()
        return [{"role": r[0], "content": r[1]} for r in rows]
```

- [ ] **Step 4: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/unit/test_session.py -v`
Expected: 5 passed (含 3 个原 + 2 个新)

- [ ] **Step 5: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/session/store.py tests/unit/test_session.py
git commit -m "feat(session): SessionStore.get_recent(limit) returns last N msgs"
```

### Task 8.3: RAG pipeline 用 get_recent

**Files:**
- Modify: `src/uni_rag/rag/pipeline.py`
- Modify: `tests/integration/test_query_pipeline.py`

- [ ] **Step 1: 写长 session 测试**

追加到 `tests/integration/test_query_pipeline.py`:

```python
def test_long_session_uses_only_recent_history(pipeline, monkeypatch):
    """30+ 轮 session，query() 只注入最近 max_session_messages 条。"""
    def fake_complete(self, system, max_tokens=1024):
        # 记录 LLM 收到的 message 数（不含 system）
        # 通过 self._messages 拿到
        n = len(self._messages)
        # 把它存到 side-effect 列表
        captured.append(n)
        return "ok"
    monkeypatch.setattr("uni_rag.llm.client.LLMClient.complete", fake_complete)

    # 设小一点方便测
    pipeline.session_store  # trigger init
    from uni_rag.config import load_settings
    settings = load_settings()
    original = settings.max_session_messages
    settings.max_session_messages = 6  # 临时改

    try:
        captured = []
        sid = "long-session"
        # 30 轮 = 60 条消息
        for i in range(30):
            pipeline.query(f"q{i}", session_id=sid)

        # 每次 query() 注入到 LLM 的 message 数应 <= max_session_messages
        # （每轮 query 自己的 user+assistant 会让 last N 多 2 条；这里只看上限）
        assert all(n <= settings.max_session_messages for n in captured)
    finally:
        settings.max_session_messages = original
```

- [ ] **Step 2: 跑测试 FAIL**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_query_pipeline.py::test_long_session_uses_only_recent_history -v`
Expected: FAIL (pipeline 现在用 get() 拿全部)

- [ ] **Step 3: 改 pipeline.query() 用 get_recent**

修改 `src/uni_rag/rag/pipeline.py` 的 `query` 方法里这段:

```python
        # 2. 加载历史
        history = []
        if session_id:
            settings = load_settings()
            history = self.session_store.get_recent(session_id, settings.max_session_messages)
```

其余保持不变。

- [ ] **Step 4: 跑测试通过**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest tests/integration/test_query_pipeline.py -v`
Expected: 3 passed

- [ ] **Step 5: 跑全套确认无回归**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest -v`
Expected: 全部 passed

- [ ] **Step 6: Commit**

```bash
cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag
git add src/uni_rag/rag/pipeline.py tests/integration/test_query_pipeline.py
git commit -m "feat(rag): pipeline.query uses get_recent with max_session_messages cap"
```

---

## Milestone 9: v0.2 验收

### Task 9.1: Coverage + BDD 端到端

- [ ] **Step 1: 跑 coverage**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest --cov=src/uni_rag --cov-report=term-missing`
Expected: coverage ≥ 90%

- [ ] **Step 2: 跑全套测试**

Run: `cd /Users/mahaoxuan/Desktop/产品项目学习/uni-rag && uv run pytest -v`
Expected: 全部 passed (v0.1: 43 tests + v0.2 新增 ~6 tests)

### Task 9.2: 手动端到端验证（PM 验收）

**M6 验证（高亮引用）：**
1. `uv run uni-rag serve`
2. 浏览器开 `http://127.0.0.1:8765/`
3. 上传 sample.pdf
4. 问 "supervised learning 是什么？"
5. 看到 assistant 答案下面有引用 chip（按钮样式）
6. 点 chip → 右侧侧栏滑出，显示原文 + 黄色高亮的命中 span
7. 按 × 或 Esc 关闭侧栏

**M7 验证（DOCX 表格+公式）：**
1. CLI: `uv run python scripts/make_table_formula_fixture.py`（已有 fixture，跳过）
2. `uv run uni-rag ingest tests/fixtures/sample_with_table_formula.docx`
3. `uv run uni-rag ask "Section 1.1 讲了什么公式？"`
4. 答案应能引用到 `x^2 + y^2 = z^2` 的 chunk

**M8 验证（长 session）：**
1. CLI 连发 30 轮：`for i in {1..30}; do uv run uni-rag ask -s test "问题 $i" > /dev/null; done`
2. 用 `sqlite3 data/sessions.db "SELECT COUNT(*) FROM messages WHERE session_id='test'"` 看总条数
3. 改 `UNI_RAG_MAX_SESSION_MESSAGES=4` 后，重复第 1 步 — 答案质量应没断崖（短 history 够覆盖追问），但 LLM 收到 context 明显变短
4. 单轮 answer 仍然完整（不是空白）

---

## 关键参考

- **v0.1 计划**: `docs/superpowers/plans/2026-06-17-uni-rag-v0.1.md`（同目录）
- **python-docx 表格 API**: `doc.tables[i].rows[r].cells[c].text`
- **CSS slide-in**: `transform: translateX(100%)` → `translateX(0)` + `transition: transform 0.25s`
- **SQLite 取最近 N 条**: `ORDER BY seq DESC LIMIT N` 子查询 + 外层 `ORDER BY seq ASC`

## 风险与回退

1. **侧栏在窄屏被裁**: max-width 90vw + transform 动画，窄屏也能用
2. **DOCX 公式误判**: `$xxx$` 在普通文本里很罕见；只有段落里完整 `$...$` 才会被识别，遗漏风险低
3. **get_recent 边界**: seq DESC + 外层 ASC 保证返回是按时间正序；limit<=0 返回空
4. **max_session_messages 太小**: 改小到 4 后多轮上下文可能丢；v0.2 暂不动态感知，由用户改 env var 调
