# VibeReader 比赛日检查清单

## 演示前 30 分钟

### 环境检查
- [ ] 网络连接稳定（MiniMax Token Plan 需要联网）
- [ ] MiniMax Token Plan 已配置真实 `MINIMAX_TOKEN_PLAN_KEY`
- [ ] 当前模型配置符合 `docs/LOCAL_MODEL_SERVICES.md`
- [ ] `demo-assets/` 文件夹在桌面可快速访问
- [ ] 备用回答文件 `demo-fallback-answer.md` 已准备

### 应用检查
- [ ] `npm run tauri:dev` 能正常启动桌面窗口
- [ ] 或者 `npm run dev` + 浏览器也能跑通
- [ ] 示例论文 `outline-demo.pdf` 能正常加载
- [ ] 大纲导航（Introduction / Methods / Findings）可点击跳转
- [ ] 选中文本后出现"注入 AI"按钮
- [ ] AI 回复能正常流式输出
- [ ] Stop 按钮在长回复时可点击中断

### 备用方案
- [ ] 网络失败时：使用 `demo-fallback-answer.md` 中的预设回答
- [ ] MiniMax 失效时：切换到 fallback 回答，不临时宣称 Kimi 可用
- [ ] PDF 渲染慢时：使用 `wonderland_short.pdf`（仅 29 页）

## 演示路径（严格计时）

| 时间 | 动作 | 检查点 |
|------|------|--------|
| 0:00-0:05 | 打开应用 | 窗口出现，零配置 |
| 0:05-0:15 | 加载 outline-demo.pdf | 左侧阅读器显示，右侧 AI 面板 |
| 0:15-0:30 | 点击 Methods 大纲 | 跳转到第 2 页 |
| 0:30-0:50 | 划选第 2 页文本，点击"注入 AI" | 右侧 Chat 出现文档上下文 |
| 0:50-1:10 | AI 回答（或展示 fallback） | 公式用 LaTeX 渲染 |
| 1:10-1:25 | 高亮文本 + 保存笔记 | 高亮可见，笔记保存 |
| 1:25-1:45 | 切换到 Summary Tab，生成摘要 | IMRAD 结构出现 |
| 1:45-2:05 | 切换到 Flashcards Tab，生成闪卡 | Q&A 卡片出现 |
| 2:05-2:25 | 切换到 Mind Map Tab，生成导图 | 树状结构出现 |
| 2:25-2:50 | 打开 sample.md，再切回 PDF | Markdown 可读，状态隔离 |
| 2:50-3:00 | 结语 | "让每一篇论文都变成你的知识资产" |

## 风险预案

### 风险 1：AI 网络请求失败
**现象**：注入后一直 loading，无响应
**应对**：
1. 等待 3 秒后说"网络有点波动，我展示预设的回答效果"
2. 切换到 `demo-fallback-answer.md` 中准备好的回答文本
3. 继续后续功能演示

### 风险 2：PDF 渲染慢
**现象**：PDF 加载超过 5 秒
**应对**：
1. 提前在后台打开应用并加载好 PDF
2. 或者使用 `wonderland_short.pdf`（小文件，加载快）

### 风险 3：MiniMax Token Plan 失效
**现象**：AI 返回 401 / 403
**应对**：
1. 检查 `MINIMAX_TOKEN_PLAN_KEY` 是否注入运行环境
2. 直接使用 fallback 回答，避免现场改接未验证模型服务

### 风险 4：演示超时
**现象**：时间超过 3 分钟
**应对**：
1. 跳过非核心功能（导图/闪卡可略）
2. 直接进入结语

## 评委可能的问题

### Q1: 和 ChatPDF / SciSpace 有什么区别？
**A**: 三个核心差异：① 本地渲染零上传，保护隐私；② 多格式支持（PDF+MD+HTML+TXT）；③ 学习闭环——不只是问答，还能生成摘要、闪卡、思维导图，让你"读完还能带走"。

### Q2: 商业模式是什么？
**A**: 短期免费开源获取用户，中期通过团队协作和高级 AI 功能收费，长期构建个人知识图谱的 SaaS 服务。

### Q3: 技术难点是什么？
**A**: 三个技术难点：① Tauri 原生 HTTP 绕过浏览器 CORS 限制，生产环境稳定通信；② pdf.js 文本解析与 Canvas 渲染的坐标对齐，实现精确高亮；③ 多文档状态隔离，8 个独立 Zustand Store 防止切换文档时状态串扰。

### Q4: 团队几个人？开发了多久？
**A**: 1 人全栈开发，7 天完成从 Tauri 壳到完整功能迭代，包含 66 个单元测试和 20 个 E2E 测试。

### Q5: 如何保护用户隐私？
**A**: 本地优先架构——PDF 用 pdf.js 本地渲染，文件不上传；AI 通信通过用户自有的 API Key 直连厂商，我们不触碰用户数据。

### Q6: 支持离线吗？
**A**: 阅读器完全离线可用。AI 功能需要联网（用户自有 Key），但阅读、批注、大纲导航、已生成的摘要/闪卡/导图全部本地存储，断网可用。
