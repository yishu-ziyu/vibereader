# VibeReader Standalone Dev 验收方法与验收矩阵

最后更新：2026-05-23

## 1. 基线验收

### 1.1 当前已验证

命令：

```bash
npm run build
```

结果：

- 通过。
- Vite 编译成功。
- 存在单 chunk 大于 500 kB 的体积警告。
- 主 bundle 约 2.27 MiB。

这些警告不是当前阻塞项，但后续需要通过 code splitting 处理。

### 1.2 每次改动后的最低验证

```bash
npm run build
```

如果已经完成 Tauri 迁移，还需要：

```bash
npm run tauri:dev
```

若做了打包相关修改，再运行：

```bash
npm run tauri:build
```

## 2. 功能验收矩阵

| 编号 | 功能 | 操作步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- | --- |
| A1 | Web 构建 | 运行 `npm run build` | 构建退出码为 0 | P0 |
| A2 | Tauri 启动 | 运行 `npm run tauri:dev` | 桌面窗口打开，主界面可见 | P0 |
| A3 | 本地 PDF 打开 | 点击打开文件，选择 PDF | PDF 文本解析成功，阅读器显示第一页 | P0 |
| A4 | PDF 翻页缩放 | 打开 PDF 后点击上一页/下一页/缩放 | 页码和画布更新，无崩溃 | P0 |
| A5 | 选区注入 AI | 在 PDF 选中文字，点击注入 | Chat 面板出现带选区内容的问题 | P0 |
| A6 | AI 流式回复 | 配置模型后发送问题 | 回复流式出现，最终消息保存 | P0 |
| A7 | Stop generating | 生成中点击停止 | 请求中断，UI 停止 loading，保留已生成文本 | P0 |
| A8 | Markdown 打开 | 打开 `.md` 文件 | 标题、列表、代码块正常渲染 | P1 |
| A9 | Markdown 选区注入 | 选中 Markdown 文本并注入 | AI 收到选中内容 | P1 |
| A10 | PDF 大纲 | 打开有目录的 PDF | 显示目录，点击可跳转 | P1 |
| A11 | 最小批注 | 选中文字，创建高亮/笔记 | 批注保存到本地存储，刷新后仍在列表 | P1 |
| A12 | 多文档切换 | 打开两个文档并切换 | 当前文档、页码、AI 上下文不串 | P1 |
| A13 | 无 API key 状态 | 清空模型配置后发送 | 显示用户可读错误或配置提示，不崩溃 | P0 |
| A14 | 离线 PDF worker | 断网或禁 CDN 后打开 PDF | PDF 仍能解析和渲染 | P1 |
| A15 | 演示脚本 | 按 `docs/DEMO_SCRIPT.md` 操作 | 3 分钟内跑通核心闭环 | P0 |

## 3. 手工验收流程

### 3.1 PDF 核心闭环

1. 启动 Tauri 桌面应用。
2. 点击“打开文件”。
3. 选择 demo PDF。
4. 等待解析进度结束。
5. 在阅读器中翻到第二页。
6. 放大到 125%。
7. 选择一段文字。
8. 点击“注入上下文”。
9. 在 AI 面板中追加问题：“用中文解释这段话，并指出它在全文中的作用。”
10. 等待 AI 回复。

通过标准：

- 阅读器没有空白。
- 选区文本进入 Chat。
- AI 回复不丢失上下文。
- 会话刷新后仍可看到历史消息。

### 3.2 通用阅读闭环

1. 打开 Markdown 文件。
2. 确认标题、列表、代码块可读。
3. 选中一段正文。
4. 注入 AI。
5. 让 AI 生成 3 张闪卡。

通过标准：

- 非 PDF 文档能进入同一阅读工作台。
- AI 能围绕选区内容回答。
- 闪卡面板能显示结果或给出明确生成入口。

### 3.3 失败状态验收

1. 打开一个非支持格式文件。
2. 打开一个损坏 PDF。
3. 清空 API key 后发送问题。
4. 网络断开时打开 PDF。

通过标准：

- 所有错误都有用户可读提示。
- 控制台不能出现持续刷屏错误。
- UI 不进入永久 loading。

## 4. 自动化测试建议

当前项目没有测试框架。建议先补最小测试，而不是一次性引入复杂 e2e。

### 4.1 单元测试优先对象

- `aiService` SSE 解析。
- `storage` 会话标题提取和 IndexedDB 封装。
- `documentService` 文件类型识别。
- `vibeParser` 结构化解析。

### 4.2 推荐工具

- Vitest：配合 Vite 迁移。
- Playwright：只做 2-3 条关键 e2e。

### 4.3 最小 e2e

1. 启动应用。
2. 打开 demo Markdown。
3. 看到正文。
4. 打开 demo PDF。
5. 看到 PDF 页码。

## 5. 验收结果记录模板

每次验证后，把结果写到 `DEVLOG.md`：

```text
## 2026-05-xx Phase N 验收

改动：
- ...

命令：
- npm run build -> pass/fail
- npm run tauri:dev -> pass/fail

手工验收：
- A1 pass
- A2 pass
- A3 fail: ...

遗留风险：
- ...
```

## 6. 发布前阻塞项

以下任一项失败，都不能说 Hackathon demo ready：

- Tauri 应用无法启动。
- 不能从系统文件选择器打开 PDF。
- PDF 阅读器空白或不能翻页。
- 选中文本不能注入 AI。
- 无 API key 时直接崩溃。
- 演示资产缺失。
- 演示脚本不能在 3 分钟内跑通。
